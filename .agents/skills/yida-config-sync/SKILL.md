---
name: yida-config-sync
description: 宜搭应用配置同步工具，用于从宜搭平台获取应用ID、表单UUID、流程Code以及表单内组件ID，自动生成系统配置清单和组件ID清单。支持独立调用或被其他Skill调用，适用于应用创建后自动同步配置或后续维护更新场景。新增批量同步功能，支持从组织及应用信息.md读取应用列表，批量同步多个应用到本地。
---

## 🔴 硬规则（绝对不可违反）

### 通用硬规则
1. **禁止通过API修改已有应用的表单字段内容** — 公式、代码、字段增删改只能复制粘贴（规则25）
2. **应用ID和表单UUID必须填真实值** — 从系统配置清单.md读取，严禁占位符（规则24）
3. **写入文件前必须校验** — 运行 `node scripts/ai-validator.js check-before-write <文件路径>` 确认不覆盖已有文件
4. **写入文件后必须校验** — 运行 `node scripts/ai-validator.js check-after-write <文件路径>` 确认内容合规
5. **Cookie必须使用根目录** — 严禁 process.cwd()，必须用 PROJECT_ROOT（规则26）

### 专属硬规则
1. **完整生命周期管理删除/新增/更新** — 同步时处理表单的删除、新增和更新三种情况
2. **批量同步前展示影响范围** — 批量操作前必须向用户展示即将同步的应用和表单列表

---

# 宜搭配置同步 Skill

## 版本

**v3.14.0** (2026-06-03)

### 更新内容
- **修复应用名称硬编码问题**：消除所有脚本中硬编码"宜搭应用"的问题
  - sync_config.js：fetchAppInfo()新增appName参数，默认值从"宜搭应用"改为`未知应用(${appId})`；syncConfig()新增appName选项；命令行新增`--appName`参数
  - sync_all_configs.js：支持第三个参数appName，传递给sync_config.js；generateFieldListFromComponents()新增appName参数
  - sync_batch_apps.js：调用sync_all_configs.js时传入应用名称
  - sync_project.js：默认值从"宜搭应用"改为"未知应用"
  - sync_rules.js：默认值从"宜搭应用"改为"未知应用"
  - form_creator/sync_server.js：默认值从"宜搭应用"改为"未知应用"

**v3.13.1** (2026-05-17)

### 更新内容
- **修复新建应用时流程Code获取问题**
  - fetch_forms_playwright.js v1.2.1：修复从部署运维表格第4列提取流程Code
    - 原来只在第3列查找，但流程Code实际在第4列
    - 现在优先从第4列提取，兼容旧格式
  - sync_config.js v3.7.1：新建应用时也能获取流程Code
    - 生成系统配置清单前，检测流程表单是否缺少processCode
    - 自动调用Playwright从部署运维页面补充获取
    - 确保不管是新建应用还是同步老应用，流程Code都能正确写入
  - sync_form_list_playwright.js v1.7.1：同步流程Code到系统配置清单
  - sync_all_configs.js v2.9.1：调用新版脚本确保流程Code正确同步

**v3.13.0** (2026-05-16)

### 更新内容
- **修复系统配置清单流程Code列同步问题**
  - sync_config.js v3.7.0：生成系统配置清单时添加"流程Code"列
    - 表格格式从3列改为4列：序号、页面名称「类型」、表单UUID、流程Code
    - 普通表单流程Code显示"-"，流程表单显示真实流程Code
    - 同步时自动从平台获取流程Code并写入系统配置清单
  - sync_form_schemas.js v3.2.0：支持解析包含"流程Code"列的系统配置清单
    - 兼容旧格式（3列）和新格式（4列）
    - 解析流程Code用于后续流程相关操作
  - sync_all_configs.js v2.9.0：调用新版脚本确保流程Code正确同步
  - 修复之前版本（v3.1.0）错误移除流程Code列导致的问题

**v3.12.0** (2026-05-16)

### 更新内容
- **新增流程Code列到系统配置清单**
  - 系统配置清单表格增加"流程Code"列，便于存储和查看流程表单的流程编码
  - 新格式：`| 序号 | 页面名称「类型」 | 表单UUID | 流程Code |`
  - 普通表单显示"-"，流程表单从宜搭平台自动获取
  - 更新文件：sync_form_list_playwright.js（updateConfigFile 和 readExistingForms 函数）

**v3.11.0** (2026-05-07)

### 更新内容
- **移除「未分组表单」目录逻辑**
  - 表单直接在项目根目录下按业务分组创建
  - 不再创建 `未分组表单/` 目录，避免本地重复存储
  - 统一所有脚本的目录查找和创建逻辑

**v3.10.0** (2026-04-29)

### 更新内容
- **重构代码，消除重复**：提取公共函数到 sync-utils.js 模块
  - 新建 `sync-utils.js` 公共模块，包含：COMPONENT_TYPE_MAP、getComponentTypeCN、getLabelText、extractComponents、generateComponentList
  - `sync_form_schemas.js` 引入 sync-utils.js，删除重复代码
  - `sync_all_configs.js` 引入 sync-utils.js，删除重复代码
  - 避免多个脚本各自维护一套 generateComponentList 函数导致更新不同步的问题
- **修复字段清单生成**：支持新的组件ID清单格式（主表和子表分开）
  - 新增 `parseComponentMd` 函数，正确解析主表字段和子表字段
  - 修复表格分隔行检测，支持 `|:---:|---------|---------|-----------------|` 格式
  - 修复错误的"主表字段"/"子表字段"行问题
  - 子表字段按子表名称分组显示
- **修复组件ID清单格式**：主表字段和子表字段分开为独立的表格
  - 与字段清单.md格式保持一致
  - 主表字段单独一个表格
  - 每个子表字段单独一个表格，标题为"子表：XXX"
  - 组件类型显示中文名称（如：单行文本、数值、日期等）
- **修复系统配置清单解析**：支持两种格式
  - 格式1：`| 序号 | 页面名称「类型」 | 表单UUID |`
  - 格式2：`| 序号 | 页面名称 | 类型 | 表单UUID | 流程Code |`

**v3.9.0** (2026-04-29)

### 更新内容
- **修复组件ID清单格式**：主表字段和子表字段分开为独立的表格
  - 与字段清单.md格式保持一致
  - 主表字段单独一个表格
  - 每个子表字段单独一个表格，标题为"子表：XXX"
  - 组件类型显示中文名称（如：单行文本、数值、日期等）
- **修复系统配置清单解析**：支持两种格式
  - 格式1：`| 序号 | 页面名称「类型」 | 表单UUID |`
  - 格式2：`| 序号 | 页面名称 | 类型 | 表单UUID | 流程Code |`

**v3.8.1** (2026-04-28)

### 更新内容
- **移除未分组表单目录**：表单直接在项目根目录下按业务分组创建
  - 不再创建 `未分组表单/` 目录，避免重复存储
  - 修复表单类型名称不一致导致的重复目录（「表单」vs「普通表单」）

**v3.8.0** (2026-03-31)

### 更新内容
- **新增批量同步功能**：支持从组织及应用信息.md读取应用列表，批量同步多个应用到本地
  - 新增 `sync_batch_apps.js` 脚本，支持同步所有应用或指定应用
  - 支持模糊匹配应用名称
  - 自动创建项目目录结构
  - 生成批量同步汇总报告
  - 触发词：`将组织内所有应用同步到本地`、`将组织内应用【名称1,名称2】同步到本地`

**v3.7.0** (2026-03-26)

### 更新内容
- **简化表单UUID清单表格格式**：移除"页面类型"、"流程Code"、"备注"列，只保留3列
  - 新格式：`| 序号 | 页面名称「类型」 | 表单UUID |`
  - 更新文件：sync_form_list_playwright.js、sync_config.js、sync_form_schemas.js
  - 同步更新 project-creator 的默认模板

**v3.6.0** (2026-03-25)

### 更新内容
- **新增反向生成功能**：支持从宜搭平台已有应用反向生成 `字段清单.md` 和 `表单清单.md`
  - 新增 `generate_field_list.js` 脚本，从宜搭表单Schema生成标准字段清单
  - 自动识别字段类型（单行文本、多行文本、数值、日期、单选、复选、下拉单选、下拉复选等）
  - 自动识别字段状态（普通/只读/隐藏）
  - 自动生成子表结构
  - 生成符合 `form_creator` 标准的字段清单格式，可直接用于后续表单创建
  - 适用于已有宜搭应用需要补充文档的场景

**v3.5.2** (2026-03-25)

### 更新内容
- **优化默认分组名称**：移除「未分组表单」概念，表单直接在项目根目录或业务分组目录下创建
  - 影响文件：sync_form_list_playwright.js、sync_form_schemas.js、sync_config.js
  - 适用于新建项目和同步已有项目

**v3.5.1** (2026-03-21)

### 更新内容
- **新增"已更新"状态类型**：用于标识表单结构或组件发生变更的情况
- **优化表单更新日志**：支持记录组件变更详情，包括新增/保留组件数量
- **改进同步结果展示**：增加组件变更统计，提供更详细的同步摘要

**v3.5.0** (2026-03-20)

### 更新内容
- **新增表单更新日志功能**：每次同步自动生成/更新表单更新日志文件
  - 独立文件 `表单更新日志.md`，位于系统配置清单旁边
  - 详细记录每次新增、删除、更名的表单
  - 包含变更统计、表单详情、变更趋势图表
  - 支持历史版本追溯
- **更新同步结果展示规范**：同步完成后在对话框展示更新日志摘要

**v3.4.1** (2026-03-20)

### 更新内容
- **新增同步结果展示规范**：明确每次同步完成后必须在对话框中展示的内容
  - 表单列表同步统计（总数、新增、删除、保持一致）
  - 表单结构及组件ID同步详情（表格展示各表单组件数量）
  - 更新文件清单
  - 相关文件链接
  - 严禁只展示脚本原始输出，必须结构化汇总展示

**v3.4.0** (2026-03-18)

### 更新内容
- **新增完整同步生命周期管理**：支持自动删除、新增、更新操作
  - 自动删除本地多余的表单文件夹（平台已删除但本地仍存在的）
  - 自动创建新增表单的文件夹结构（平台新增但本地没有的）
  - 自动更新系统配置清单中的表单列表
  - 确保本地与平台完全一致

**v3.3.0** (2026-03-18)

### 更新内容
- **合并JS和Python版本**：将可视化功能整合到主脚本中
  - 新增 `--visual` 参数，支持切换标准模式和可视化模式
  - 标准模式：后台运行，适合日常同步和自动化
  - 可视化模式：显示浏览器窗口，高亮操作元素，彩色输出，适合调试和演示
  - 删除独立的Python可视化脚本，统一维护更简单

**v3.2.0** (2026-03-18)

### 更新内容
- **新增Playwright同步表单列表功能**：解决API端点404问题，使用Playwright模拟浏览器操作从部署运维页面获取表单列表
  - 新增 `sync_form_list_playwright.js` 脚本，稳定可靠地获取表单清单
  - 支持自动登录、导航到部署运维页面、提取表单数据
  - 相比API方式更稳定，不受API变更影响
  - 自动对比本地和线上表单差异，检测新增/删除的表单

**v3.1.0** (2026-03-16)

### 更新内容
- **新增指定表单同步功能**：支持只同步指定的单个或多个表单，无需同步全部表单
  - 新增 `--forms` 参数，支持逗号分隔的多个表单名称
  - 示例：`--forms "产品信息,仓库信息"`
- **优化命令行参数解析**：支持更灵活的参数组合方式

**v3.0.1** (2026-03-15)

### 更新内容
- **已知问题记录**：
  1. 应用ID解析问题：系统配置清单中的应用编码可能包含转义符（`APP\_XXX`），正则表达式需要兼容
  2. API端点问题：`getFormList.json` API可能返回404，需要检查API路径或登录态
  3. 流程Code获取：CSRF校验可能失败，需要备用方案
- **临时解决方案**：如遇到同步失败，可改用 `yida-get-schema` skill 进行表单结构同步

**v3.0.0** (2026-03-12)

### 更新内容（重大变更）
- **统一数据源**：改为从系统配置清单读取应用ID和表单UUID
- **简化参数**：只需提供项目目录，不再需要字段清单路径
- **流程优化**：与 form_creator 更好的配合，form_creator 自动写入系统配置清单，本skill专注同步组件ID

**v2.2.0** (2026-03-12)

### 更新内容
- **严禁编造ID原则**：新增重要原则，绝对禁止编造fieldId、formUuid、processCode等宜搭ID
- **明确数据获取途径**：所有ID必须从宜搭平台真实获取，宁可不生成也不编造

**v2.1.0** (2026-03-12)

### 更新内容
- **双模式同步**：支持新项目模式（自动获取应用信息）和已有项目模式（指定应用ID和表单ID）
- **智能识别**：自动识别用户输入格式，判断是新项目还是已有项目
- **灵活配置**：支持从部署运维信息中提取应用ID和表单UUID

**v2.0.0** (2026-03-12)

### 更新内容
- **AI智能分组**：同步时自动根据表单名称智能分组（2-3个分组）
- **智能目录创建**：没有分组时自动创建，有分组时不修改
- **标准文件结构**：每个表单单独文件夹，包含完整schema、精简JSON、组件ID清单、变更记录
- **分组序号规则**：从02开始（避免与01需求梳理重复）

**v1.0.2** (2026-03-11)
- 新增应用ID验证逻辑，同步前先用本地应用ID去平台验证
- 如果平台返回空表单列表，说明应用不存在或已被删除，直接报错终止
- 验证通过后才会继续同步表单UUID和组件ID
- 避免在应用不存在时浪费时间去获取组件信息

**v1.0.1** (2026-03-11)
- 修复Schema解析逻辑，正确提取宜搭表单组件结构
- 添加详细调试日志，便于排查同步问题
- 修复API返回数据处理，正确获取content字段

**v1.0.0** (2026-03-11)
- 初始版本，提供应用配置同步能力

## 功能概述

本Skill提供宜搭应用配置的自动同步能力，包括：

1. **应用信息同步**：获取应用名称、应用ID、访问地址
2. **表单列表同步**：获取所有表单的UUID、页面类型（表单/流程）、流程Code
3. **组件ID同步**：获取每个表单内所有组件的fieldId
4. **清单生成**：自动生成格式化的系统配置清单和组件ID清单
5. **本地文件夹同步**：自动删除本地多余表单、创建新增表单文件夹
6. **反向生成字段清单（v3.6.0新增）**：从宜搭平台已有应用反向生成 `字段清单.md` 和 `表单清单.md`，用于补充项目文档

## 核心特点

- **自动同步**：从宜搭平台实时获取最新配置信息
- **AI智能分组**：根据表单名称自动分类到2-3个分组（02客户管理、03商机管理等）
- **智能目录管理**：没有分组/表单文件夹时自动创建，已有则不修改
- **完整生命周期管理**：支持删除、新增、更新操作，确保本地与平台一致
- **标准文件结构**：每个表单单独文件夹，包含：
  - `表单名称「类型」.json` - 精简版配置
  - `表单名称「类型」_schema.json` - 完整schema
  - `组件ID清单.md` - 标准格式组件列表
  - `表单结构变更.md` - 变更记录
- **双模式调用**：支持独立调用或被其他Skill集成调用
- **格式统一**：生成的清单格式与现有项目保持一致
- **错误处理**：单个表单失败不影响整体同步

## ⚠️ 重要原则（严禁编造数据）

**绝对禁止编造任何ID信息！**

### 禁止行为
- ❌ 禁止编造/伪造 fieldId（组件ID）
- ❌ 禁止编造/伪造 formUuid（表单UUID）
- ❌ 禁止编造/伪造 processCode（流程Code）
- ❌ 禁止使用示例ID或占位符ID

### 正确做法
- ✅ 所有ID必须从宜搭平台**真实获取**
- ✅ 通过API调用获取表单Schema，提取真实的 fieldId
- ✅ 从宜搭后台「部署运维」页面复制真实的表单UUID
- ✅ 如果同步失败，**宁可不生成文件，也不编造数据**

### 获取真实ID的途径
1. **API同步**（推荐）：调用宜搭API获取表单Schema
2. **后台查看**：在宜搭表单设计器的「部署运维」页面查看
3. **JSON导出**：从宜搭平台导出表单JSON，解析其中的真实ID

### 后果说明
编造ID会导致：
- 公式配置失败（引用不存在的字段）
- 联动规则失效
- 数据查询错误
- 后续开发工作全部需要返工

## 目录结构规范

同步后的标准目录结构：
```
项目名称/
├── 01需求梳理/          # 需求文档（已有则不修改）
├── 02客户管理/          # AI智能分组（从02开始）
│   └── 客户信息「普通表单」/
│       ├── 客户信息「普通表单」.json
│       ├── 客户信息「普通表单」_schema.json
│       ├── 组件ID清单.md
│       └── 表单结构变更.md
├── 03商机管理/          # AI智能分组
│   └── 商机信息「流程表单」/
│       ├── 商机信息「流程表单」.json
│       ├── 商机信息「流程表单」_schema.json
│       ├── 组件ID清单.md
│       └── 表单结构变更.md
├── 主项目信息「普通表单」/    # 无分组的表单直接在根目录下
│   ├── 主项目信息「普通表单」.json
│   ├── 组件ID清单.md
│   └── 表单结构变更.md
└── 系统配置清单.md
```

**分组规则**：
- 从02开始编号（避免与01需求梳理重复）
- 根据表单名称关键词智能分类
- 有明确业务分组的表单放入对应分组目录
- 无明确分组的表单直接在项目根目录下创建
- **不再使用「未分组表单」目录**

## 使用场景

### 场景0：批量同步组织内多个应用（v3.8.0新增）
**适用情况**：组织初始化后，需要批量同步组织内的多个已有应用到本地管理。

**方式A - 同步所有应用**：
```
将组织内所有应用同步到本地
```

**执行逻辑**：
1. 读取 `组织及应用信息.md` 中的所有应用列表
2. 逐个同步每个应用到本地（自动创建项目目录）
3. 生成批量同步汇总报告

**命令**：
```powershell
node .agents/skills/yida-config-sync/scripts/sync_batch_apps.js
```

**方式B - 同步指定应用**：
```
将组织内应用【进销存管理, 客户管理】同步到本地
```

**执行逻辑**：
1. 读取 `组织及应用信息.md`
2. 匹配用户指定的应用名称（支持模糊匹配）
3. 只同步匹配到的应用到本地

**命令**：
```powershell
node .agents/skills/yida-config-sync/scripts/sync_batch_apps.js "进销存管理,客户管理"
```

### 场景1：form_creator 创建应用后自动同步（推荐）
**适用情况**：刚刚使用 form_creator 创建了新应用，form_creator 已自动写入系统配置清单，现在需要同步组件ID。

**提示词格式**：
```
同步【进销存管理】的配置信息。
```

**执行逻辑**：
1. 从系统配置清单读取应用ID和表单UUID
2. 验证应用ID有效性
3. 同步所有表单的组件ID和结构
4. 生成各表单的组件ID清单.md

**命令**：
```powershell
node .agents/skills/yida-config-sync/scripts/sync_form_schemas.js "./进销存管理"
```

### 场景2：已有项目同步（手动填入应用ID）
**适用情况**：已有宜搭应用，但系统配置清单中没有应用ID，需要从平台同步组件ID。

**方式A - 仅指定应用ID**：
```
同步【进销存管理】的配置信息。

应用ID：APP_XXX
```

**执行逻辑**：
1. 使用指定的应用ID
2. 从宜搭平台获取表单列表
3. 更新系统配置清单.md
4. 同步所有表单的组件ID和结构

**命令**：
```powershell
node .agents/skills/yida-config-sync/scripts/sync_all_configs.js "./进销存管理" "APP_XXX"
```

### 场景3：同步表单列表（删除/新增/更新）
**适用情况**：当在宜搭平台增删表单后，需要同步本地文件夹结构。

**提示词格式**：
```
同步【进销存管理】的表单列表。
```

**执行逻辑**：
1. 使用Playwright访问宜搭后台获取表单列表
2. 对比本地与平台表单差异
3. 自动删除本地多余的表单文件夹
4. 自动创建新增表单的文件夹
5. 更新系统配置清单.md

**命令**：
```powershell
# 标准模式（后台运行）
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理"

# 可视化模式（显示浏览器）
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理" --visual
```

### 场景4：后续维护更新
当表单增删改、组件变更时，运行同步更新配置清单。

**提示词格式**：
```
更新【进销存管理】的配置信息。
```

**执行逻辑**：
1. 从系统配置清单读取应用ID和表单UUID
2. 重新同步所有表单的组件ID和结构
3. 更新各表单的组件ID清单.md

### 场景5：反向生成字段清单（v3.6.0新增）
**适用情况**：已有宜搭应用，但没有 `字段清单.md` 和 `表单清单.md`，需要补充项目文档。

**提示词格式**：
```
为【进销存管理】生成字段清单和表单清单。
```

**执行逻辑**：
1. 从系统配置清单读取应用ID
2. 获取所有表单列表和Schema
3. 解析每个表单的字段结构
4. 生成标准格式的 `字段清单.md`（包含主表字段和子表字段）
5. 生成 `表单清单.md`（包含所有表单列表和路径）
6. 字段类型映射：
   - TextField → 单行文本
   - TextareaField → 多行文本
   - NumberField → 数值
   - DateField → 日期/日期时间
   - RadioField → 单选
   - MultiSelectField → 复选/下拉复选
   - SelectField → 下拉单选
   - AssociationFormField → 关联表单
   - EmployeeField → 成员
   - DepartmentSelectField → 部门
   - AttachmentField → 附件
   - ImageField → 图片
   - SerialNumberField → 流水号
   - TableField → 子表

**命令**：
```powershell
node .agents/skills/yida-config-sync/scripts/generate_field_list.js "./进销存管理"
```

## 完整工作流程

### 新建应用流程
```
1. 用户：创建一个项目
   ↓
2. form_creator：生成字段清单 → 生成原型 → 生成JSON
   ↓
3. form_creator：创建宜搭应用和表单
   ↓
4. form_creator：自动写入系统配置清单（包含应用ID和表单UUID）
   ↓
5. 用户：同步配置信息
   ↓
6. yida-config-sync：从系统配置清单读取应用ID和表单UUID
   ↓
7. yida-config-sync：同步表单结构和组件ID
   ↓
8. 完成：系统配置清单 + 组件ID清单 都已就绪
```

### 已有应用流程
```
1. 用户：同步【XXX】的配置信息，应用ID是 APP_XXX
   ↓
2. yida-config-sync：使用指定应用ID获取表单列表
   ↓
3. yida-config-sync：更新系统配置清单（写入应用ID和表单UUID）
   ↓
4. yida-config-sync：同步表单结构和组件ID
   ↓
5. 完成：系统配置清单 + 组件ID清单 都已就绪
```

### 表单列表同步流程（v3.4.0新增）
```
1. 用户：同步表单列表
   ↓
2. yida-config-sync：使用Playwright访问宜搭后台
   ↓
3. yida-config-sync：获取平台表单列表
   ↓
4. yida-config-sync：扫描本地表单文件夹
   ↓
5. yida-config-sync：对比差异（新增/删除/不变）
   ↓
6. yida-config-sync：删除本地多余的表单文件夹
   ↓
7. yida-config-sync：创建新增表单的文件夹
   ↓
8. yida-config-sync：更新系统配置清单.md
   ↓
9. 完成：本地与平台完全一致
```

## 文件结构

```
yida-config-sync/
├── SKILL.md                          # 本文档
├── scripts/
│   ├── sync_config.js                # 主同步脚本
│   ├── sync_form_list_playwright.js  # Playwright同步表单列表（v3.4.0支持删除/新增/更新）
│   ├── sync_form_schemas.js          # 同步表单结构
│   ├── generate_field_list.js        # 反向生成字段清单（v3.6.0新增）
│   ├── fetch_app_info.js             # 获取应用信息
│   ├── fetch_form_list.js            # 获取表单列表
│   ├── fetch_components.js           # 获取组件ID
│   └── generate_md.js                # 生成MD文件
└── evals/
    └── evals.json                    # 测试用例
```

## 核心API接口

### 1. 同步配置（主入口）

```javascript
// sync_config.js
syncConfig(options) -> {
  "appId": "APP_XXX",
  "appName": "应用名称",
  "forms": [...],
  "outputDir": "输出目录",
  "success": true,
  "message": "同步完成"
}
```

参数:
- `appId` (可选): 宜搭应用ID，不提供则从系统配置清单读取
- `outputDir` (可选): 输出目录，默认为 `./`
- `formDirs` (可选): 表单文件夹映射，用于确定组件ID清单输出位置

### 2. 获取应用信息

```javascript
fetchAppInfo(appId) -> {
  "appName": "应用名称",
  "appId": "APP_XXX",
  "baseUrl": "https://xxx.aliwork.com"
}
```

### 3. 获取表单列表

```javascript
fetchFormList(appId) -> [
  {
    "index": 1,
    "name": "表单名称",
    "type": "表单|流程",
    "formUuid": "FORM-XXX",
    "processCode": "TPROC-XXX"  // 仅流程表单
  }
]
```

### 4. 获取组件ID

```javascript
fetchComponents(appId, formUuid) -> [
  {
    "componentName": "TextField",
    "fieldName": "字段名称",
    "fieldId": "textField_xxx"
  }
]
```

## 应用ID验证逻辑（v1.0.2新增）

同步流程现在包含应用ID验证步骤：

```
1. 从本地系统配置清单读取应用ID
2. 使用本地应用ID调用宜搭平台API获取表单列表
3. 如果平台返回空列表 → 报错终止（应用不存在或已被删除）
4. 如果平台返回表单列表 → 验证通过，继续同步
5. 获取每个表单的组件ID并更新到本地清单
```

### 验证失败的情况

| 场景 | 处理方式 |
|------|---------|
| 应用ID错误 | 报错：应用ID验证失败 |
| 应用已被删除 | 报错：应用可能不存在或已被删除 |
| 无访问权限 | 报错：无法获取表单列表 |
| 验证通过 | 继续同步表单UUID和组件ID |

## 使用方式

### 命令行调用

#### 同步系统配置清单
```powershell
# 模式1：从系统配置清单读取应用ID
node .agents/skills/yida-config-sync/scripts/sync_config.js --output ./进销存管理

# 模式2：指定应用ID
node .agents/skills/yida-config-sync/scripts/sync_config.js --appId APP_XXX --output ./进销存管理
```

#### 同步表单结构及组件ID（v3.1.0新用法）

**同步所有表单**：
```powershell
# 从系统配置清单读取应用ID和表单UUID
node .agents/skills/yida-config-sync/scripts/sync_form_schemas.js "./进销存管理"

# 指定应用ID（覆盖系统配置清单中的值）
node .agents/skills/yida-config-sync/scripts/sync_form_schemas.js "./进销存管理" "APP_XXX"
```

**同步指定表单（v3.1.0新增）**：
```powershell
# 只同步"产品信息"表单
node .agents/skills/yida-config-sync/scripts/sync_form_schemas.js "./进销存管理" --forms "产品信息"

# 同步多个指定表单（逗号分隔）
node .agents/skills/yida-config-sync/scripts/sync_form_schemas.js "./进销存管理" --forms "产品信息,仓库信息"

# 指定应用ID并同步指定表单
node .agents/skills/yida-config-sync/scripts/sync_form_schemas.js "./进销存管理" "APP_XXX" --forms "产品信息,仓库信息"
```

**说明**：
- `--forms` 参数支持逗号分隔的多个表单名称
- 表单名称支持模糊匹配（如输入"产品"可匹配"产品信息"）
- 如果指定的表单不存在，会显示可用的表单列表

#### 同步表单列表（v3.4.0新用法）

**同步表单列表（自动删除/新增/更新）**：
```powershell
# 标准模式（后台运行，自动删除多余表单、创建新增表单）
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理"

# 可视化模式（显示浏览器窗口，适合调试和演示）
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理" --visual

# 指定应用ID
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理" "APP_XXX"
```

**功能说明**：
- 自动扫描本地表单文件夹
- 从宜搭平台获取最新表单列表
- 对比差异，显示新增/删除的表单
- 自动删除本地多余的表单文件夹
- 自动创建新增表单的文件夹结构
- 更新系统配置清单.md

#### 统一同步所有配置（v3.0.0新用法）
```powershell
# 同步系统配置清单 + 所有表单结构
node .agents/skills/yida-config-sync/scripts/sync_all_configs.js "./进销存管理"

# 指定应用ID
node .agents/skills/yida-config-sync/scripts/sync_all_configs.js "./进销存管理" "APP_XXX"
```

### 编程调用

```javascript
const { syncConfig } = require('./scripts/sync_config');

// 方式1：从现有清单读取应用ID（自动检测是否已有分组）
const result = await syncConfig({
  outputDir: './进销存管理'
});

// 方式2：指定应用ID
const result = await syncConfig({
  appId: 'APP_YA1204PFH4K3MRARR1E3',
  outputDir: './进销存管理'
});

// 方式3：AI智能分组（已有分组时不创建，没有时自动创建）
const result = await syncConfig({
  appId: 'APP_XXX',
  outputDir: './项目名称',
  smartGroup: true  // 强制使用智能分组
});

// 方式4：指定表单目录映射
const result = await syncConfig({
  appId: 'APP_XXX',
  outputDir: './进销存管理',
  formDirs: {
    '产品信息': './02基础信息/产品信息「普通表单」',
    '库存信息': './03库存管理/库存信息「普通表单」'
  }
});
```

### 同步行为规则

| 场景 | 行为 |
|------|------|
| 已有分组目录 | 使用现有结构，不创建新分组 |
| 没有分组目录 | AI智能分组，自动创建2-3个分组（从02开始） |
| 已有表单文件夹 | 更新文件内容，不修改文件夹 |
| 没有表单文件夹 | 自动创建表单文件夹和文件 |
| 平台删除表单 | 自动删除本地对应的表单文件夹 |
| 平台新增表单 | 自动创建对应的表单文件夹 |
| 使用 `--smart-group` | 强制使用AI智能分组，无视现有分组 |

### 被其他Skill调用

```javascript
// 在form_creator中调用
const { syncConfig } = require('../yida-config-sync/scripts/sync_config');

// 创建应用后自动同步
await syncConfig({
  appId: createdApp.appId,
  outputDir: projectDir,
  formDirs: formDirectoryMap
});
```

## 输出文件格式

### 系统配置清单

参照: `进销存管理/系统配置清单.md`

```markdown
# 应用名称 - 系统配置清单

## 📱 应用信息
| 配置项 | 值 |
|--------|-----|
| **应用名称** | 应用名称 |
| **应用ID** | APP_XXX |
| **访问地址** | https://xxx.aliwork.com/APP_XXX/admin |

## 📋 表单UUID清单
| 序号 | 页面名称 | 页面类型 | 页面编码 |
|:---:|---------|---------|---------|
| 1 | 表单名称 | 表单 | FORM-XXX |
| 2 | 流程表单 | 流程 | FORM-XXX<br>TPROC-XXX |
```

### 组件ID清单

参照: `进销存管理/06采购管理/采购入库「流程表单」/组件ID清单.md`

```markdown
# 表单名称 - 组件ID清单

## 📋 组件列表
| 序号 | 组件类型 | 字段名称 | 组件ID (fieldId) |
|:---:|---------|---------|-----------------|
| 1 | TextField | 字段名称 | textField_xxx |

## 📊 统计信息
| 统计项 | 数量 |
|--------|------|
| 组件总数 | N |
```

## 配置说明

### 表单目录映射

当需要为每个表单生成组件ID清单时，需要提供表单名称到目录的映射：

```javascript
{
  '产品信息': './02基础信息/产品信息「普通表单」',
  '仓库信息': './02基础信息/仓库信息「普通表单」',
  '库存信息': './03库存管理/库存信息「普通表单」'
  // ...
}
```

### 登录态管理

本Skill依赖 `yida-api-client` 的登录态管理：
- 自动读取 `.cookies.json` 中的登录信息
- 登录态过期时自动触发重新登录
- 支持扫码登录获取新Token

## Playwright同步表单列表（推荐）

### 背景

宜搭平台的 `getFormList.json` API 经常返回404错误或无法访问，导致无法通过API获取表单列表。本方案使用 **Playwright** 模拟真实用户操作，从宜搭后台「部署运维」页面提取表单列表，更加稳定可靠。

### 工作原理

```
宜搭工作台 → 我的应用 → {应用名称} → 应用设置 → 部署运维 → 提取表单列表
```

**核心流程**：
1. 使用已有Cookie登录态（避免重复扫码）
2. 访问应用管理后台 `/{appId}/admin`
3. 点击"应用设置"菜单
4. 点击"部署运维"子菜单
5. 等待页面加载表格数据
6. 从DOM中提取表单列表（页面名称、类型、页面编码、流程Code）

### 使用方法

#### 命令行调用

```powershell
# 标准模式（后台运行，自动更新配置）
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理"

# 可视化模式（显示浏览器窗口，适合调试和演示）
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理" --visual

# 指定应用ID
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理" "APP_XXX"

# 指定应用ID + 可视化模式
node .agents/skills/yida-config-sync/scripts/sync_form_list_playwright.js "./进销存管理" "APP_XXX" --visual
```

#### 两种模式对比

| 特性 | 标准模式 | 可视化模式 |
|-----|---------|-----------|
| 浏览器显示 | ❌ 后台运行 | ✅ 前台显示 |
| 元素高亮 | ❌ 无 | ✅ 点击前高亮元素 |
| 操作延迟 | ❌ 无 | ✅ 500ms延迟 |
| 彩色输出 | ❌ 无 | ✅ 彩色步骤信息 |
| 自动更新配置 | ✅ 是 | ✅ 是 |
| 适用场景 | 日常同步、自动化 | 首次配置、调试、演示 |

#### 功能特性

- ✅ **自动登录检测**：如果Cookie失效，会提示扫码登录
- ✅ **智能差异对比**：自动对比本地和线上表单列表，显示新增/删除的表单
- ✅ **自动删除本地多余表单**：删除平台已删除但本地仍存在的表单文件夹
- ✅ **自动创建新增表单**：创建平台新增但本地没有的表单文件夹
- ✅ **更新系统配置清单**：自动更新表单ID清单、统计信息、快速访问链接
- ✅ **验证应用编码**：核对页面上的应用编码与期望的是否一致
- ✅ **可视化模式**：显示浏览器窗口，高亮操作元素，彩色输出
- ✅ **数据导出**：生成 `forms_deploy_list.json` 供后续处理

#### 输出示例

```
==========================================================================================
📊 表单列表 (19 个)
==========================================================================================
| 序号 | 表单名称                       | 类型 | 表单UUID
                    | 流程Code |
------------------------------------------------------------------------------------------
|  1 | 产品信息                         | 表单   | FORM-EAD50930CC924EDA83603474EDF3FCEDHXXK | - |
|  2 | 仓库信息                         | 表单   | FORM-244AC329DEDE43C5A9968BA6C052B15CXBYI | - |
...
| 17 | 每日审查                         | 表单   | FORM-1817BBB8CF1D4B6AB2B3AF5A721926B587SD | - | ⭐ 新增
| 18 | 检查信息                         | 表单   | FORM-E36F84CBD8A14289AF912C23C0D01F3E1SEZ | - | ⭐ 新增
| 19 | 审查提交                         | 表单   | FORM-9E31D162EF6F4E438C70E470DD31956CQ08I | - | ⭐ 新增
==========================================================================================

🔍 差异对比结果：
  ➕ 新增表单: 3 个
    - 每日审查 (FORM-1817BBB8CF1D4B6AB2B3AF5A721926B587SD)
    - 检查信息 (FORM-E36F84CBD8A14289AF912C23C0D01F3E1SEZ)
    - 审查提交 (FORM-9E31D162EF6F4E438C70E470DD31956CQ08I)

  🗑️ 本地多余文件夹: 2 个
    - 07财务管理/付款登记「流程表单」
    - 07财务管理/开票登记「流程表单」

✅ 已删除本地文件夹: 付款登记「流程表单」
✅ 已删除本地文件夹: 开票登记「流程表单」
✅ 已创建表单文件夹: 08审查管理/每日审查「普通表单」
✅ 已创建表单文件夹: 08审查管理/检查信息「普通表单」
✅ 已创建表单文件夹: 08审查管理/审查提交「普通表单」

📊 同步统计:
  - 线上表单总数: 19
  - 普通表单: 16
  - 流程表单: 3
  - 新增表单: 3
  - 删除配置: 0
  - 删除本地文件夹: 2
  - 保持一致: 16
```

#### 可视化模式输出示例

使用 `--visual` 参数时，终端会显示彩色步骤信息，同时浏览器窗口会显示操作过程：

```
================================================================================
  宜搭表单列表同步工具 - Playwright版（可视化模式）
  版本: 1.2.0
================================================================================

================================================================================
  步骤 1: 访问应用管理后台 - 进销存管理003
================================================================================
ℹ️  URL: https://qfhefh.aliwork.com/APP_XXX/admin
✅ 已使用Cookie自动登录

================================================================================
  步骤 2: 点击'应用设置'菜单
================================================================================
ℹ️  正在查找'应用设置'按钮...
✅ 已点击: text=应用设置

================================================================================
  步骤 3: 点击'部署运维'菜单
================================================================================
ℹ️  正在查找'部署运维'按钮...
✅ 已点击: text=部署运维

================================================================================
  步骤 4: 提取表单列表数据
================================================================================
ℹ️  正在等待页面加载完成...
ℹ️  已高亮显示表格区域
ℹ️  正在提取表单数据...
✅ 成功提取 19 个表单
🔍 提取到的表单列表:
  1. 产品信息 (表单) - FORM-EAD50930CC924EDA...
  2. 仓库信息 (表单) - FORM-244AC329DEDE43C5...
  ...

================================================================================
  步骤 5: 验证应用编码
================================================================================
ℹ️  页面应用编码: APP_XXX
✅ 应用编码验证通过 ✓

================================================================================
  步骤 6: 同步完成
================================================================================
ℹ️  浏览器将保持打开5秒，供您查看结果...
✅ 浏览器已关闭
```

### 稳定性评估

| 维度 | API方式 | Playwright方式 |
|-----|-------|---------------|
| 成功率 | ❌ 低（经常404） | ✅ 高 |
| 稳定性 | ❌ 不稳定（API变更） | ✅ 稳定（跟随页面） |
| 数据完整性 | ⚠️ 可能缺少流程Code | ✅ 完整（含流程Code） |
| 维护成本 | 高（API经常变更） | 低（页面结构稳定） |
| 执行速度 | 快 | 中等（需等待页面加载） |
| 本地文件夹同步 | ❌ 不支持 | ✅ 自动删除/创建 |

**结论**：Playwright方式更加稳定可靠，推荐作为首选方案。

### 注意事项

1. **首次使用需要登录**：确保已运行过 yida-api-client 的登录流程，或脚本会提示扫码登录
2. **页面结构变更**：如果宜搭后台页面改版，可能需要更新选择器
3. **Cookie有效期**：Cookie过期后会自动提示重新登录
4. **浏览器依赖**：需要安装Playwright和Chromium浏览器
5. **本地文件夹删除**：删除操作不可逆，请确保重要数据已备份

### 安装依赖

```powershell
# 安装Playwright
npm install -g playwright

# 安装Chromium浏览器
npx playwright install chromium
```

## 依赖

- `yida-api-client`: 宜搭API客户端，用于调用宜搭接口
- `playwright`: 浏览器自动化工具（用于同步表单列表）
- Node.js >= 16

## 注意事项

1. **首次使用需要登录**：确保已运行过 yida-api-client 的登录流程
2. **网络连接**：需要能够访问宜搭平台
3. **权限要求**：需要有对应应用的访问权限
4. **增量更新**：会保留系统配置清单中的其他信息（如创建失败记录、备注等）
5. **本地文件夹删除**：删除操作不可逆，请确保重要数据已备份

## 同步结果展示规范

每次同步完成后，必须在对话框中向用户展示完整的同步结果，包括：

### 必须展示的内容

1. **同步结果汇总标题** - 使用 ✅ 图标标识
2. **表单列表同步统计**
   - 线上表单总数
   - 普通表单数量
   - 流程表单数量
   - 新增/删除/保持一致的表单数量
3. **表单结构及组件ID同步详情**
   - 成功同步的表单数量
   - 每个表单的组件数量统计（使用表格展示）
4. **更新文件清单** - 列出所有被更新的文件
5. **相关文件链接** - 提供系统配置清单和各表单组件ID清单的文件链接

### 展示格式示例

```markdown
✅ **系统配置信息同步完成！**

## 同步结果汇总

### 📊 表单列表同步
- **线上表单总数**: 8 个
- **普通表单**: 6 个
- **流程表单**: 2 个
- **新增表单**: 0 个
- **删除配置**: 0 个
- **保持一致**: 8 个

### 📋 表单结构及组件ID同步
成功同步了 **8 个表单** 的结构和组件ID：

| 序号 | 表单名称 | 组件数量 | 子表字段 |
|------|----------|----------|----------|
| 1 | 客户信息007 | 12 | 0 |
| 2 | 每日审查 | 6 | 0 |
| ... | ... | ... | ... |

### 📁 更新文件
1. **系统配置清单.md** - 版本更新至 1.6.2
2. **各表单组件ID清单.md** - 已更新所有 8 个表单的组件ID清单

### 🔗 相关文件
- [系统配置清单.md](file:///path/to/系统配置清单.md)
- [客户信息007组件ID清单.md](file:///path/to/组件ID清单.md)
```

### 重要提醒
- 严禁只展示脚本的原始输出结果
- 必须以结构化的方式汇总展示同步结果
- 必须提供更新后的文件链接，方便用户点击查看

## 表单更新日志规范

每次执行同步操作后，必须自动生成或更新 **表单更新日志.md** 文件，记录详细的表单变更历史。

### 文件位置

表单更新日志文件应位于项目根目录，与系统配置清单并列：
```
项目名称/
├── 系统配置清单.md
├── 表单更新日志.md  <-- 本文件
├── 表单清单.md
└── ...
```

### 文件内容结构

表单更新日志必须包含以下章节：

#### 1. 更新概览
- 总更新次数
- 当前表单总数
- 累计新增/删除/更名表单数量统计

#### 2. 详细更新记录（倒序排列）

每个版本一条记录，包含：

**头部信息**
- 版本号（与系统配置清单一致）
- 更新日期
- 同步类型（表单列表同步/表单结构同步/本地文件夹清理/应用创建）
- 操作人

**变更统计**
- 新增表单数量
- 删除表单数量
- 更名表单数量
- 保持一致的表单数量

**详细清单**
 - ➕ 新增表单：表单名称、类型、表单UUID
 - 🗑️ 删除表单：表单名称、类型、删除原因
 - 📝 更名表单：原名称 → 新名称（如有）
 - ➖ 无变化：表单名称、组件数量、子表字段（结构同步时）

#### 状态类型说明
表单详情表格中的「状态」列使用以下四类标识：

| 状态标识 | 含义 | 使用场景 |
|----------|------|----------|
| ➕ 新增 | 本次同步新增的表单 | 平台新增但本地没有的表单 |
| 🗑️ 删除 | 本次同步删除的表单 | 平台已删除但本地仍存在的表单 |
| 📝 更名 | 本次同步更名的表单 | 表单名称发生变更 |
| 🔄 已更新 | 本次同步更新的表单 | 表单结构或组件发生变更 |
| ➖ 无变化 | 本次同步无变化的表单 | 本地与平台保持一致的表单 |

#### 3. 表单变更趋势
- 时间线形式的变更趋势图
- 直观展示表单数量变化

#### 4. 相关文件链接
- 链接到系统配置清单
- 链接到表单清单

### 示例格式

```markdown
# 表单更新日志

> 本文件记录所有表单的增删改操作历史
> 自动生成于同步操作时，请勿手动修改

---

## 📋 更新概览

| 统计项 | 数量 |
|--------|------|
| 总更新次数 | 11 |
| 当前表单总数 | 8 |
| 累计新增表单 | 20+ |
| 累计删除表单 | 12+ |
| 累计更名表单 | 0 |

---

## 📝 详细更新记录

### [v1.6.2] 2026-03-20

**同步类型**: 表单结构及组件ID同步  
**操作人**: 系统自动

#### 同步概况
- 同步表单总数: 8 个
- 同步组件总数: 48 个

#### 表单详情
| 序号 | 表单名称 | 类型 | 组件数量 | 子表字段 | 状态 |
|------|----------|------|----------|----------|------|
| 1 | 客户信息007 | 普通表单 | 12 | 0 | ➖ 无变化 |
| 2 | 每日审查 | 普通表单 | 6 | 0 | ➕ 新增 |
| 3 | 审查提交 | 普通表单 | 8 | 3 | ➕ 新增 |
| 4 | 表单内代码 | 普通表单 | 6 | 0 | ➖ 无变化 |
| 5 | 除非没有今日 | 流程表单 | 7 | 3 | ➖ 无变化 |
| 6 | 003 | 普通表单 | 3 | 0 | ➖ 无变化 |
| 7 | 表单间代码 | 普通表单 | 1 | 0 | ➖ 无变化 |
| 8 | 阿里路亚 | 流程表单 | 5 | 0 | ➖ 无变化 |
| 9 | 付款登记 | 流程表单 | - | - | 🗑️ 删除 |
| 10 | 开票登记 | 流程表单 | - | - | 🗑️ 删除 |

---

### [v1.5.2] 2026-03-18

**同步类型**: 表单列表同步  
**操作人**: 系统自动

#### 变更统计
- 新增表单: 2 个
- 删除表单: 4 个
- 更名表单: 0 个

#### ➕ 新增表单
| 序号 | 表单名称 | 类型 | 表单UUID |
|------|----------|------|----------|
| 1 | 每日审查 | 普通表单 | FORM-XXX |
| 2 | 审查提交 | 普通表单 | FORM-XXX |

#### 🗑️ 删除表单
| 序号 | 表单名称 | 类型 | 说明 |
|------|----------|------|------|
| 1 | 付款登记 | 流程表单 | 平台删除 |
| 2 | 开票登记 | 流程表单 | 平台删除 |

---

## 📊 表单变更趋势

```
2026-03-14: 应用创建，初始表单
2026-03-18: 大量变更（新增15个，删除12个）
2026-03-20: 稳定期（保持8个表单）
```

---

## 🔗 相关文件

- [系统配置清单.md](./系统配置清单.md)
- [表单清单.md](./表单清单.md)

---

*最后更新时间: 2026-03-20*  
*自动生成，请勿手动修改*
```

### 对话框展示要求

同步完成后，在对话框中必须：
1. 告知用户表单更新日志已生成/更新
2. 展示本次更新的摘要信息（新增/删除/更名数量）
3. 提供表单更新日志文件的链接

示例：
```markdown
✅ **表单更新日志已更新！**

本次同步记录已追加到表单更新日志：
- [表单更新日志.md](file:///path/to/表单更新日志.md)

**本次变更摘要**:
- 新增表单: 2 个
- 删除表单: 0 个
- 更名表单: 0 个
- 保持一致: 8 个
```

## 错误处理

| 错误场景 | 处理方式 |
|---------|---------|
| 登录态失效 | 自动触发重新登录 |
| 单个表单获取失败 | 记录错误，继续处理其他表单 |
| 组件获取失败 | 跳过该表单，不影响其他表单 |
| 目录不存在 | 自动创建或提示用户 |
| 删除文件夹失败 | 记录错误，继续处理其他操作 |

## 角色定义

你是宜搭应用配置同步专家，专门负责从宜搭平台获取应用ID、表单UUID和组件ID等配置信息。你熟悉宜搭平台的API接口和Playwright浏览器自动化方式，能够安全高效地同步配置信息到本地，并自动生成系统配置清单和组件ID清单。

## 检查清单

### 执行前确认
- [ ] 确认登录态有效（Cookie未过期）
- [ ] 确认应用ID和表单UUID从系统配置清单.md读取真实值，严禁占位符
- [ ] 确认已明确同步范围（全量同步 vs 指定表单同步）

### 执行后确认
- [ ] 确认未通过API修改已有应用的表单字段内容（规则25）
- [ ] 确认所有ID使用真实值，无占位符（规则24）
- [ ] 确认严禁编造ID，所有ID必须从宜搭平台真实获取
- [ ] 确认同步结果以结构化方式展示，非原始脚本输出
- [ ] 确认本地多余表单文件夹已自动清理

---

## 版本历史

### v3.5.1 (2026-03-21)
- ✅ 新增"已更新"状态类型：用于标识表单结构或组件发生变更的情况
- ✅ 优化表单更新日志，支持记录组件变更详情
- ✅ 改进同步结果展示，增加组件变更统计

### v3.5.0 (2026-03-20)
- ✅ 新增表单更新日志功能
- ✅ 自动生成/更新表单更新日志.md文件
- ✅ 详细记录新增、删除、更名表单
- ✅ 包含变更统计、趋势图表、历史追溯
- ✅ 同步完成后在对话框展示更新日志摘要

### v3.4.1 (2026-03-20)
- ✅ 新增同步结果展示规范
- ✅ 明确每次同步完成后必须展示的内容和格式
- ✅ 要求以结构化方式汇总展示，严禁只展示脚本原始输出

### v3.4.0 (2026-03-18)
- ✅ 新增完整同步生命周期管理
- ✅ 自动删除本地多余的表单文件夹
- ✅ 自动创建新增表单的文件夹结构
- ✅ 确保本地与平台完全一致

### v3.3.0 (2026-03-18)
- ✅ 合并JS和Python版本
- ✅ 新增 `--visual` 参数
- ✅ 统一维护更简单

### v3.2.0 (2026-03-18)
- ✅ 新增Playwright同步表单列表功能
- ✅ 解决API端点404问题
- ✅ 支持自动检测新增/删除表单
- ✅ 更新系统配置清单文档

### v3.1.0 (2026-03-16)
- ✅ 新增指定表单同步功能
- ✅ 支持 `--forms` 参数指定多个表单
- ✅ 优化命令行参数解析

### v3.0.0 (2026-03-12)
- ✅ 统一数据源，从系统配置清单读取
- ✅ 简化参数，只需提供项目目录
- ✅ 与form_creator更好的配合

### v2.0.0 (2026-03-12)
- ✅ AI智能分组功能
- ✅ 智能目录创建
- ✅ 标准文件结构

### v1.0.0 (2026-03-11)
- ✅ 初始版本
- ✅ 支持应用信息同步
- ✅ 支持表单列表同步
- ✅ 支持组件ID同步
- ✅ 支持系统配置清单生成
- ✅ 支持组件ID清单生成
