---
name: form_creator
description: 根据用户自然语言描述或Markdown字段表生成宜搭表单JSON配置，支持三种独立场景：1)创建新应用+所有表单（触发词："生成表单"、"创建应用"）→ 使用 create_from_markdown.js；2)向已有应用添加单个表单（触发词："在XX应用中创建一个表单"、"在XX应用中添加表单"）→ 使用 add_form_to_app.js；3)仅创建项目目录结构（触发词："创建项目"、"新建XXX系统"）→ 不调用此skill。v6.12.0新增明确的触发规则区分。Invoke when user explicitly says "生成表单" or "在XX应用中创建一个表单". Must NOT invoke when user only says "创建项目" or "新建系统".
---

## 🔴 硬规则（绝对不可违反）

### 通用硬规则
1. **禁止通过API修改已有应用的表单字段内容** — 公式、代码、字段增删改只能复制粘贴（规则25）
2. **应用ID和表单UUID必须填真实值** — 从系统配置清单.md读取，严禁占位符（规则24）
3. **写入文件前必须校验** — 运行 `node scripts/ai-validator.js check-before-write <文件路径>` 确认不覆盖已有文件
4. **写入文件后必须校验** — 运行 `node scripts/ai-validator.js check-after-write <文件路径>` 确认内容合规
5. **Cookie必须使用根目录** — 严禁 process.cwd()，必须用 PROJECT_ROOT（规则26）

### 专属硬规则
1. **saveFormSchema仅允许对新建表单调用** — 严禁对已有表单调用saveFormSchema
2. **创建失败必须回滚已创建的表单** — 避免留下空白表单
3. **同名表单跳过不删除** — 检测到同名表单时跳过创建，不删除后重建

---

# 表单创建 Skill

## ⚠️ 重要警告（v6.8.0新增）

### ❌ 禁止自动调用场景（非常重要！）

**当用户说以下话时，绝对不要自动调用此skill：**

| 用户说的话 | 正确的处理方式 | 错误的处理方式 |
|-----------|--------------|--------------|
| "创建一个项目" | 只创建项目目录结构和字段清单.md | ❌ 不要自动生成JSON和宜搭应用 |
| "新建一个项目叫XXX" | 只创建项目文件夹和基础文档 | ❌ 不要调用form_creator |
| "帮我搭建一个项目" | 询问用户是否需要生成表单 | ❌ 不要自动执行生成脚本 |
| "我要做进销存" | 先了解需求，再询问是否生成表单 | ❌ 不要直接生成所有表单 |
| "初始化XXX系统" | 只创建项目目录结构 | ❌ 不要调用form_creator |

### ✅ 允许调用场景（必须满足明确的触发词）

**场景1：创建新应用 + 所有表单（使用 create_from_markdown.js）**

| 用户说的话 | 说明 | 调用的脚本 |
|-----------|------|-----------|
| "根据这个字段清单生成表单" | 用户提供了字段清单文件，明确要求生成 | `create_from_markdown.js` |
| "帮我把这些字段生成宜搭表单" | 明确要求生成表单配置 | `create_from_markdown.js` |
| "生成JSON文件并创建到宜搭" | 明确要求双模式执行 | `create_from_markdown.js` |
| "用流程图生成表单" | 提供了流程图，要求生成表单 | `create_from_markdown.js` |
| "创建应用" + 提到表单 | 创建新应用并生成表单 | `create_from_markdown.js` |

**场景2：向已有应用添加单个表单（使用 add_form_to_app.js）**

| 用户说的话 | 说明 | 调用的脚本 |
|-----------|------|-----------|
| **"在XX应用中创建一个表单"** | **向已有应用添加单个表单** | **`add_form_to_app.js`** |
| **"在XX应用中添加表单"** | **向已有应用添加单个表单** | **`add_form_to_app.js`** |
| **"给XX应用增加一个表单"** | **向已有应用添加单个表单** | **`add_form_to_app.js`** |
| **"在XX应用里新建表单"** | **向已有应用添加单个表单** | **`add_form_to_app.js`** |
| **"直接创建这个表单"（在已有应用目录下）** | **上下文感知：在已有应用中创建** | **`add_form_to_app.js`** |

**⚠️ 重要：上下文感知规则**

**判断原则：**
1. **"创建这个表单"** → 指上下文提到的**单个表单**，应使用 `add_form_to_app.js` 添加到当前应用
2. **"创建应用" / "生成所有表单"** → 用户提供**完整字段清单**（多个表单），才使用 `create_from_markdown.js` 创建新应用

**具体判断逻辑：**

| 用户说的话 | 上下文特征 | 应使用的脚本 |
|-----------|-----------|-------------|
| "创建这个表单" / "直接创建" | 已设计单个表单 | `add_form_to_app.js` |
| "生成表单" / "创建应用" | 提供完整字段清单（多表单） | `create_from_markdown.js` |
| "在XX应用中添加表单" | 明确指定已有应用 | `add_form_to_app.js` |

**AI判断流程：**
```
用户说"创建表单"
    ↓
检查上下文
    ↓
是"这个表单"（单个）还是"应用/所有表单"（多个）？
    ↓ 单个                    ↓ 多个
使用 add_form_to_app.js   使用 create_from_markdown.js
（向当前应用添加）         （创建新应用+所有表单）
```

**示例1 - 向已有应用添加：**
- 用户在 `d:\项目\叶秋功能测试\未分组表单` 目录下设计表单
- 用户说："直接创建这个表单"
- AI应该：使用 `add_form_to_app.js --appId APP_XXX` 创建到叶秋功能测试应用

**示例2 - 创建新应用：**
- 用户提供完整的字段清单.md（包含多个表单）
- 用户说："根据这个清单创建应用"
- AI应该：使用 `create_from_markdown.js` 创建新应用和所有表单

### 🔑 关键区分原则

| 场景 | 触发关键词 | 上下文特征 | 调用的脚本 |
|------|-----------|-----------|-----------|
| **场景1：仅创建项目** | "创建项目"、"新建系统"、"搭建项目" | 无表单相关 | ❌ **不调用此skill** | 无 |
| **场景2：创建新应用+所有表单** | "生成表单"、"创建应用" | 提供完整字段清单（多表单） | `create_from_markdown.js` |
| **场景3：向已有应用添加单个表单** | "创建**这个**表单"、"直接创建"<br>"在XX应用中添加表单" | 已设计单个表单<br>或在已有应用目录下 | `add_form_to_app.js` |

**核心判断逻辑：**
```
用户说"创建..."
    ↓
关键词是什么？
    ↓
"这个表单" / "直接创建" → 使用 add_form_to_app.js（单个表单）
"应用" / "所有表单" → 使用 create_from_markdown.js（新应用+多表单）
"在XX应用中" → 使用 add_form_to_app.js（指定应用）
```

**记忆口诀：**
- **"这个"**表单 = 单个 = 添加到当前应用
- **"应用"** / **"所有"**表单 = 多个 = 创建新应用
- **"在XX应用中"** = 明确指定 = 添加到指定应用

### 🔑 关键原则

1. **用户说"创建项目" ≠ 生成表单**
   - 创建项目 = 创建目录 + 编写需求文档
   - 生成表单 = 执行脚本 + 创建宜搭应用

2. **必须询问确认**
   - 如果用户没有明确说"生成表单"，必须先询问
   - 例如："您是要先创建项目结构，还是要直接生成宜搭表单？"

3. **分阶段执行**
   - 第一阶段：创建项目目录 + 字段清单
   - 第二阶段（用户确认后）：生成JSON + 创建宜搭应用

4. **区分创建新应用 vs 向已有应用添加表单**
   - **创建新应用**：使用 `create_from_markdown.js`，会创建新应用和所有表单
   - **向已有应用添加表单**：使用 `add_form_to_app.js`，只添加单个表单到指定应用

### 🔑 关键原则

1. **用户说"创建项目" ≠ 生成表单**
   - 创建项目 = 创建目录 + 编写需求文档
   - 生成表单 = 执行脚本 + 创建宜搭应用

2. **必须询问确认**
   - 如果用户没有明确说"生成表单"，必须先询问
   - 例如："您是要先创建项目结构，还是要直接生成宜搭表单？"

3. **分阶段执行**
   - 第一阶段：创建项目目录 + 字段清单
   - 第二阶段（用户确认后）：生成JSON + 创建宜搭应用

---

## ⚠️ 严禁编造ID（v6.9.0新增）

**绝对禁止编造任何宜搭ID信息！**

### ❌ 禁止行为
- 禁止编造/伪造 fieldId（组件ID）
- 禁止编造/伪造 formUuid（表单UUID）
- 禁止编造/伪造 processCode（流程Code）
- 禁止使用示例ID、占位符ID或随机生成的ID

### ✅ 正确做法
- 所有ID必须从宜搭平台**真实获取**
- 通过API调用获取表单Schema，提取真实的 fieldId
- 从宜搭后台「部署运维」页面复制真实的表单UUID
- 如果无法获取真实ID，**宁可不生成文件，也不编造数据**

### 🔍 如何获取真实ID
1. **API同步**（推荐）：调用宜搭API获取表单Schema
2. **后台查看**：在宜搭表单设计器的「部署运维」页面查看
3. **JSON导出**：从宜搭平台导出表单JSON，解析其中的真实ID

### ⚡ 后果说明
编造ID会导致：
- 公式配置失败（引用不存在的字段）
- 联动规则失效
- 数据查询错误
- 后续开发工作全部需要返工
- 严重影响项目进度和质量

---

## 角色定义

你是宜搭表单创建专家，专门负责根据字段清单生成宜搭表单JSON配置并创建到宜搭平台。你熟悉宜搭表单的JSON Schema规范和API接口，能够安全高效地创建新应用和表单。

## 检查清单

### 执行前确认
- [ ] 确认登录态有效（Cookie未过期）
- [ ] 确认应用ID和表单UUID从系统配置清单.md读取真实值，严禁占位符
- [ ] 确认已区分创建场景（新应用+所有表单 vs 向已有应用添加单个表单）
- [ ] 确认每个表单的流水号字段不超过1个

### 执行后确认
- [ ] 确认未通过API修改已有应用的表单字段内容（规则25）
- [ ] 确认所有ID使用真实值，无占位符（规则24）
- [ ] 确认saveFormSchema仅对新建表单调用，未对已有表单调用
- [ ] 确认创建失败时已回滚已创建的表单
- [ ] 确认同名表单已跳过而非删除重建
- [ ] 确认已自动删除临时文件（field_config.js等）

---

## 版本更新说明

**当前版本：v6.25.0** (2026-06-03)

### 更新内容
- 🐛 **【重要修复】删除表单后原型页面仍显示空表单**：
  - **根因**：同步应用时只删除了本地文件夹，没有更新 `form-config.js` 中的 `staticConfigData` 和 `formPaths`，也没有更新 `字段清单.md`。`generatePrototypePages()` 在原型页面已存在时直接跳过，导致 `form-config.js` 永远不会被更新
  - **修复1**：新增 `regenerateFormConfigJs()` 函数，当表单列表变化时（新增/删除），仅重新生成 `form-config.js` 而不重新生成整个原型页面
  - **修复2**：`generatePrototypePages()` 新增 `formListChanged` 参数，原型页面已存在但表单列表变化时，调用 `regenerateFormConfigJs()` 更新
  - **修复3**：删除表单后自动从 `字段清单.md` 中移除对应的表单章节
  - **修复4**：`prototype_generator.js` 新增 `--form-config-only` 参数，支持仅重新生成 `form-config.js`
  - **修复5**：`loadFormListFromConfig()` 新增清理逻辑，从系统配置清单动态解析有效表单，移除 `formPaths`、`staticConfigData`、`FormConfigData` 中已不存在的表单
  - **修复6**：`app.js` 的 `DOMContentLoaded` 中自动调用 `loadFormListFromConfig()` 并重新渲染菜单

**历史版本：v6.24.0** (2026-06-03)

### 更新内容
- ✅ **【新增功能】创建新应用后自动生成原型页面**：
  - 在 `create_from_markdown.js` 中新增 `generatePrototype` 函数
  - 应用和表单创建完成后，自动调用 `form-to-prototype` skill 生成原型页面
  - 原型页面生成位置：`{项目目录}/01需求梳理/原型页面/`
  - 自动更新组织及应用信息.md，追加原型页面访问地址
  - 用户可在浏览器中预览表单界面，验证字段设计

**历史版本：v6.22.0** (2026-05-07)

### 更新内容
- ✅ **【结构优化】简化组织及应用信息.md中的应用列表表格结构**：
  - 应用列表从5列简化为3列：序号、应用名称、应用 ID (appId)
  - 移除"应用类型"和"备注"列，简化维护工作
  - 同步更新代码逻辑，支持新的3列表格结构
  - 代码保持向后兼容，支持动态列数检测

**历史版本：v6.21.0** (2026-05-07)

### 更新内容
- ✅ **【重要修复】修复组织及应用信息更新失败和JSON文件未生成的问题（create_from_markdown.js v2.2.0）**：
  - 修复正则表达式匹配，支持"应用 ID"（带空格）和"应用ID"（无空格）两种格式
  - 新增 `syncFormSchemas` 函数，在同步完成后自动调用 `yida-get-schema` 生成JSON文件
  - 确保创建完成后所有本地文件完整：系统配置清单、组件ID清单、变更记录、JSON文件
  - 解决组织及应用信息.md中的应用列表未更新的问题
  - 解决表单目录中缺少JSON文件的问题

**历史版本：v6.20.0** (2026-05-07)

### 更新内容
- ✅ **【重要修复】修复新应用创建后同步失败的问题（create_from_markdown.js v2.1.0 + sync_config.js v3.5.0）**：
  - 创建完成后将表单信息写入 `.temp_forms.json` 临时文件
  - 调用 yida-config-sync 时传递 `createdForms` 参数
  - 同步脚本优先使用已知的表单UUID，跳过API查询表单列表
  - 应用验证阶段识别 `createdForms`，跳过API验证
  - 解决新应用API返回404导致同步失败的问题

**历史版本：v6.19.0** (2026-05-07)

### 更新内容
- ✅ **【重要重构】简化 create_from_markdown.js 为 v2.0.0**：
  - 不再提前创建本地文件（JSON、组件ID清单、变更记录）
  - 只负责在宜搭平台创建应用和表单
  - 创建完成后调用 yida-config-sync 整体同步（当作已有应用处理）
  - 本地所有文件由 yida-config-sync 统一生成，确保数据一致性
  - 彻底解决了"先创建本地文件再同步"导致的重复变更记录问题
  - 移除了以下不再需要的函数：createLocalFormDirectories、generateInitialComponentList、generateInitialFormJson、generateInitialChangeLog、syncAllFormSchemas、updateComponentListFromJson、generateSystemConfig、findFormDirectory

### 更新内容
- ✅ **【重要】修复 form_manager.js 重复创建问题**：
  - 修改 form_manager.js v1.4.0：检测到同名表单时跳过创建，而不是删除后重新创建
  - 解决了字段ID不断变化的根本原因
- ✅ **优化工作流程：不需要先生成本地JSON**：
  - 正确流程：直接创建宜搭表单 → 创建完成后同步回来 → 以宜搭为准
  - 不需要 `generate_from_markdown.js` 先生成本地JSON（本地JSON中的字段ID是假的）
  - 创建完成后使用 `yida-config-sync` 同步真实的字段ID
  - 复用已有的同步老应用能力

**历史版本：v6.17.2** (2026-05-07)

### 更新内容
- ✅ **新增问题6：form_manager.js 删除旧表单重新创建**：
  - 记录 form_manager.js v1.3.0 的严重问题：检测到同名表单时删除后重新创建
  - 分析根本原因：第278-296行的 deleteForm 逻辑导致字段ID不断变化
  - 提供正确的工作流程：创建一次 → 同步回来 → 以宜搭为准
  - 提供3种解决方案：修改 form_manager.js、创建后手动同步、使用 yida-config-sync

**历史版本：v6.17.1** (2026-05-06)

### 更新内容
- ✅ **新增问题3：表单在宜搭平台重复创建（多次重复）**：
  - 分析根本原因：宜搭表单创建是"两步式"的（先创建空白表单，再保存Schema）
  - 如果保存Schema失败，空白表单已经留在平台上
  - 提供3种解决方案：创建前检查、失败时自动删除、手动清理
  - 记录典型场景和表格说明

**历史版本：v6.17.0** (2026-05-06)

### 更新内容
- ✅ **新增常见问题预防与解决方案章节**：
  - **问题1：宜搭流水号组件限制** - 每个表单最多只能有1个流水号，记录典型场景和解决方案
  - **问题2：表单重复创建（本地目录重复）** - 分析 generate_from_markdown.js 和 create_from_markdown.js 目录逻辑不一致导致的重复创建问题
  - **问题3：关联表单字段配置** - 需要手动配置关联关系
  - **问题4：流程表单流程设计** - 需要手动设计审批流程

**历史版本：v6.16.0** (2026-04-04)

### 更新内容
- ✅ **修复上下文感知触发规则（重要修复）**：
  - 新增"上下文感知规则"，解决在已有应用目录下说"创建表单"时错误创建新应用的问题
  - **核心原则**：
    - "创建**这个**表单" → 指上下文提到的**单个表单** → 使用 `add_form_to_app.js`
    - "创建**应用**" / "生成**所有**表单" → 用户提供**完整字段清单**（多表单）→ 使用 `create_from_markdown.js`
  - 新增判断流程图和记忆口诀，帮助AI快速准确判断
  - 在场景2触发词列表中增加"创建这个表单"、"直接创建"
  - 更新关键区分原则表格，增加"上下文特征"列

**历史版本：v6.15.0** (2026-03-31)

### 更新内容
- ✅ **修复 add_form_to_app.js 脚本（v1.3.0）**：
  - **智能目录定位**：字段清单在 01需求梳理/ 时，表单目录自动创建在项目根目录
  - **检查表单目录是否存在**：如果 form_designer 已创建过表单目录，直接更新文件而不重新创建
  - **修复嵌套目录问题**：避免在表单目录内再创建嵌套目录
  - **智能更新文件**：只更新需要更新的文件（JSON、组件ID清单、变更记录）
  - 新增 getCorrectProjectRoot() 函数，正确判断项目根目录

**历史版本：v6.14.0** (2026-03-28)

### 更新内容
- ✅ **修复 add_form_to_app.js 脚本（v1.2.0）**：
  - 修复表单目录创建位置：现在在用户指定的文件夹（字段清单所在目录）中创建
  - 修复项目根目录查找逻辑，支持从字段清单文件所在目录向上查找系统配置清单.md
  - 新增自动创建表单目录结构功能（表单名称「类型」/）
  - 新增自动同步JSON文件功能（调用 yida-get-schema）
  - 新增自动更新系统配置清单功能
  - 修复组件ID清单生成问题
  - 现在向已有应用添加表单时，会自动创建完整的本地目录结构

**历史版本：v6.13.0** (2026-03-27)

### 更新内容
- ✅ **自动同步组件ID**：应用和表单创建完成后，自动同步表单结构和组件ID到本地
  - 解决form_planner规划表单后组件ID缺失的问题
  - create_from_markdown.js 创建完成后自动调用 yida-config-sync 同步组件ID
  - 无需手动执行同步命令，创建完成后立即可用于编写公式和代码提示词
  - 防止AI在后续操作中编造组件ID导致错误

**历史版本：v6.12.0** (2026-03-25)

### 更新内容
- ✅ **优化单个表单创建触发规则**：明确区分三种使用场景
  - **场景1 - 创建新项目**：用户说"创建项目"、"新建XXX系统" → 只创建目录结构，不调用此skill
  - **场景2 - 创建新应用+所有表单**：用户说"生成表单"、"创建应用" → 使用 `create_from_markdown.js`
  - **场景3 - 向已有应用添加单个表单**：用户说"在XX应用中创建一个表单"、"在XX应用中添加表单" → 使用 `add_form_to_app.js`
  - 三种场景完全独立，触发词不重叠，避免混淆

**历史版本：v6.11.0** (2026-03-25)

### 更新内容
- ✅ **新增向已有应用添加单个表单功能**：使用 `add_form_to_app.js` 脚本
  - 支持向已有宜搭应用添加单个表单（普通表单或流程表单）
  - 触发关键词："在XX应用中创建一个表单"、"在XX应用中添加表单"
  - 自动同步表单结构和组件ID到本地
  - 与创建新应用的功能完全区分，避免混淆
  - **v1.1.0更新**：修复了本地目录结构创建问题，现在会自动创建完整的表单目录

**历史版本：v6.10.0** (2026-03-25)

### 更新内容
- ✅ **自动更新组织及应用信息**：应用创建完成后，自动将应用信息添加到 `组织及应用信息.md`
  - **【重要】只写入应用基本信息**：应用名称、应用ID、应用类型、创建时间
  - **【重要】禁止写入表单详情**：不要把表单列表、表单UUID、字段信息等写入此文件
  - 新应用自动插入到列表第1位（序号为1）
  - 其他应用序号自动顺延
  - 自动更新最后更新时间
  - 避免重复添加已存在的应用

### ⚠️ 组织及应用信息.md 文件内容规范（v6.20.0新增）

**文件用途**：记录组织级别的应用列表，用于管理多个应用的基本信息

**允许写入的内容**：
| 内容类型 | 示例 | 说明 |
|---------|------|------|
| 应用名称 | 项目管理系统 | 应用的显示名称 |
| 应用ID | APP_E93FFH596VLVFWWG7SKJ | 宜搭平台的应用标识 |
| 应用类型 | 普通应用 | 普通应用/流程应用等 |
| 创建时间 | 2026-05-07 | 应用创建日期 |
| 备注 | 含27个表单 | 简要备注，不包含详细列表 |

**禁止写入的内容**：
| 内容类型 | 示例 | 说明 |
|---------|------|------|
| 表单详细列表 | 各表单的名称、UUID | 应写入项目目录下的系统配置清单.md |
| 字段信息 | 字段ID、字段类型 | 应写入各表单目录下的组件ID清单.md |
| 流程配置 | 流程Code、审批节点 | 应写入项目目录下的配置文件 |

**正确的文件结构示例**：
```markdown
## 应用列表

| 序号 | 应用名称 | 应用 ID (appId) | 应用类型 | 备注 |
|------|----------|-----------------|----------|------|
| 1 | 项目管理系统 | APP_E93FFH596VLVFWWG7SKJ | 普通应用 | 2026-05-07创建 |
| 2 | 进销存管理 | APP_XXXXXXXXXXXXXXXX | 普通应用 | 2026-05-06创建 |
```

**错误的文件结构示例**：
```markdown
❌ 不要这样写：
## 项目管理系统表单清单
### 基础信息
| 表单名称 | 表单类型 | 表单UUID |
|----------|----------|----------|
| 机构信息 | 普通表单 | FORM-XXX |
...
```

**表单详情应该存放的位置**：
- 项目目录下的 `系统配置清单.md` - 记录所有表单的UUID和基本信息
- 各表单目录下的 `组件ID清单.md` - 记录表单内字段的组件ID

**历史版本：v6.9.2** (2026-03-14)

### 更新内容
- ✅ **修复系统配置清单表格格式问题**：优化Markdown表格渲染，避免格式混乱
  - UUID使用代码格式（反引号包裹），防止长字符串导致表格错位
  - 优化表头对齐方式
  - 缩短流程Code显示（「待从后台获取」→「待获取」）
  - 保留完整表单名称（包含「普通表单」「流程表单」后缀），确保插件能正确识别表单类型

**历史版本：v6.9.1** (2026-03-13)

### 更新内容
- ✅ **修复流水号字段必填问题**：SerialNumberField 强制 validation 为空数组，确保流水号永不必填
- ✅ **更新字段清单模板**：所有字段默认非必填（便于前期测试数据）

**历史版本：v6.9.0** (2026-03-12)

### 更新内容
- ✅ **新增严禁编造ID原则**：绝对禁止编造fieldId、formUuid、processCode等宜搭ID
- ✅ **明确ID获取途径**：所有ID必须从宜搭平台真实获取
- ✅ **强调后果严重性**：编造ID会导致公式失效、联动失败、数据错误

**历史版本：v6.8.0** (2026-03-12)

### 更新内容
- ✅ **修复 PowerShell 命令格式**：将所有 `bash` 代码块改为 `powershell`，避免 `&&` 分隔符错误
- ✅ **统一命令示例格式**：所有命令示例使用 PowerShell 兼容格式，每行命令独立

**历史版本：v6.7.0** (2026-03-12)

### 更新内容
- ✅ **新增重要警告说明**：明确区分"创建项目"和"生成表单"的场景，禁止在创建项目时自动调用此skill
- ✅ **明确调用条件**：只有用户明确要求生成表单时才可调用
- ✅ **分阶段执行原则**：先创建项目结构，用户确认后再生成表单

**历史版本：v6.6.0** (2026-03-12)

### 更新内容
- ✅ **自动生成提示词文件**：每个表单目录下自动生成 `公式/公式提示词.md` 和 `代码/代码提示词.md`
- ✅ **预填充字段信息**：提示词模板自动包含表单字段列表，方便后续生成公式和代码
- ✅ **标准化模板格式**：采用统一的提示词格式，便于AI理解和处理

**历史版本：v6.5.0** (2026-03-12)
- ✅ **自动同步表单结构**：应用创建完成后自动调用 yida-get-schema，同步线上表单结构并覆盖本地JSON
- ✅ **JSON文件带真实ID**：同步后的JSON文件包含宜搭平台生成的真实fieldId，可直接用于复制到其他页面/应用
- ✅ **完整组件结构**：同步结果包含所有组件类型（表单字段、布局组件、自定义组件）

**历史版本：v6.4.0** (2026-03-11)
- ✅ **自动同步配置信息**：应用创建完成后自动调用 yida-config-sync，生成系统配置清单和组件ID清单
- ✅ **集成配置同步**：自动获取应用ID、表单UUID、流程Code和组件fieldId
- ✅ **自动生成清单**：在项目根目录生成系统配置清单.md，在各表单目录生成组件ID清单.md

**历史版本：v6.3.0** (2026-03-11)
- ✅ **双模式同步执行**：JSON生成和静默创建现在**同时执行**，不再二选一
- ✅ **优化执行流程**：先执行 generate_from_markdown.js 生成本地JSON，再执行 create_from_markdown.js 静默创建到宜搭平台
- ✅ **统一输出结果**：一次命令执行，同时获得本地JSON文件和宜搭平台应用

**历史版本：v6.2.0** (2026-03-11)
- ✅ **新增静默创建功能**：支持从字段清单直接创建应用到宜搭平台
- ✅ **集成yida-api-client**：调用宜搭API自动创建应用和表单
- ✅ **支持Cookie持久化**：首次扫码登录后，后续自动复用登录态

**历史版本：v6.1.0** (2026-03-10)
- ✅ **支持表格格式字段清单**：字段名称、字段类型、字段说明、字段状态、是否必填
- ✅ **支持字段状态**：编辑/只读/隐藏，自动映射到宜搭behavior属性
- ✅ **支持必填验证**：根据"是否必填"列自动生成validation规则
- ✅ **集成yida_field_templates.js v4.1.0**：所有22种字段类型支持status属性

## 功能说明

这个Skill让用户可以通过**自然语言描述**、**流程图**或**Markdown字段表**，自动创建宜搭应用和表单，并同步到本地。

### 简化执行流程（v6.19.0重要更新）

**从v6.19.0开始，执行流程大幅简化**：

```
字段清单.md
    ↓
【唯一步骤】执行 create_from_markdown.js v2.0.0
    ↓
1. 解析字段清单
2. 创建宜搭应用
3. 创建所有表单
4. 更新关联字段配置
5. 更新组织及应用信息
6. 调用 yida-config-sync 整体同步（当作已有应用处理）
    ↓
【完成】
    ✅ 宜搭平台在线应用（可立即使用）
    ✅ 本地所有文件由 yida-config-sync 统一生成（确保数据一致性）
```

**核心原则：先在宜搭创建，再同步回来。创建完成后就是"已有应用"，使用同步已有应用的逻辑。**

**不再需要的步骤（v6.19.0已移除）：**
- ❌ 不再需要 `generate_from_markdown.js` 先生成本地JSON
- ❌ 不再需要提前创建本地文件（JSON、组件ID清单、变更记录）
- ❌ 不再需要单独同步每个表单

### 支持的执行模式

- **单表模式**：生成单个表单（可以包含子表），输出到 `单表/` 目录，同时创建到宜搭平台
- **项目模式**：生成多个相关表单，自动组织到项目目录结构，同时创建到宜搭平台

---

## 重要区分规则（非常重要，必须严格遵守！）

### 单表模式 vs 项目模式

| 模式 | 输出位置 | 说明 |
|------|----------|------|
| **单表模式** | `单表/` 目录 | 生成单个表单文件（该表单可以包含子表，如订单表+订单明细子表） |
| **项目模式** | `项目/项目名称/` 目录 | 生成多个独立的表单文件，按业务模块组织 |

### 判断逻辑（必须遵循）

**单表模式触发条件**（满足任一）：
- 用户明确说"单表"
- 用户只提到一个表单（不管有没有子表）
- 用户说"生成一个XX表"
- 用户提供单个表单的字段列表（含子表字段）
- 用户说"生成到单表目录"
- 从Excel读取单个表单的内容

**项目模式触发条件**（满足任一）：
- 用户提到"系统"、"项目"
- 用户提到多个表单
- 用户提供流程图/思维导图
- 用户提到"模块"
- 用户说"生成多个表单"
- 用户提供Markdown字段表文件路径

---

## 目录结构规范（重要更新）

### 源文件与生成目标分离

为了避免误删用户源文件，采用**源文件目录**和**生成目标目录**完全分离的结构：

| 目录类型 | 路径 | 用途 |
|---------|------|------|
| **源文件目录** | `流程及字段梳理/项目名称/` | 存放用户提供的Markdown字段表、流程图等源文件 |
| **生成目标目录** | `项目/项目名称/` | 存放生成的宜搭表单JSON文件 |

### 源文件目录结构

```
AI编写宜搭字段V1.1/                      # 项目根目录
├── 流程及字段梳理/                        # 源文件根目录
│   ├── 出入库管理/                       # 项目文件夹
│   │   └── 出入库管理.md                  # 字段表源文件（Markdown格式）
│   ├── 进销存管理/
│   │   └── 进销存管理.md
│   └── 下单管理/
│       └── 下单管理流程图.drawio
└── ...
```

### 输出文件结构

**重要规则：优先使用用户指定的输出目录！**

#### 情况1：用户指定了输出目录（推荐）
如果用户明确说"生成到xxx目录"或提供了输出路径，**直接使用该目录**：

```
{用户指定目录}/                       # 用户指定的输出目录
├── 基础信息/                         # 模块文件夹
│   ├── 产品类别「普通表单」.json
│   ├── 产品信息「普通表单」.json
│   └── 仓库信息「普通表单」.json
├── 库存管理/
│   ├── 采购入库「流程表单」.json
│   ├── 销售出库「流程表单」.json
│   └── 库存盘点「普通表单」.json
└── 表单清单.md                       # 自动生成的表单清单
```

**关键规则：模块目录与 01需求梳理 在同一层级**
- 字段清单位置：`{项目目录}/01需求梳理/字段清单.md`
- 表单输出位置：`{项目目录}/`（直接输出到项目目录，不创建额外子目录）
- 业务模块目录（如 02基础信息、03库存管理）直接放在项目目录下

**示例：**
- 字段清单：`d:\宜搭AI编程\宜搭AI助手V1.1\进销存管理\01需求梳理\字段清单.md`
- 执行命令：`node generate_from_markdown.js "进销存管理/01需求梳理/字段清单.md" "进销存管理"`
- 输出位置：`d:\宜搭AI编程\宜搭AI助手V1.1\进销存管理\02基础信息\`（与 01需求梳理 同级）

#### 情况2：用户未指定输出目录（默认）
如果用户没有指定输出目录，使用默认结构：

```
03项目交付物/{项目名称}/       # 默认项目目录
├── 01需求梳理/                # 假设源文件在此
├── 02基础信息/                # 生成的模块目录
├── 03库存管理/
└── 表单清单.md
```

**说明**：
- **优先使用用户指定的目录**（如果用户提供了）
- 用户未指定时，项目模式默认输出到 `03项目交付物/{项目名称}/`
- **禁止**自动创建 `03-表单配置` 等额外子目录
- 业务模块目录直接放在项目目录下，与 01需求梳理 同级
- 单表模式输出到 `02宜搭场景案例库/宜搭表单/`（独立参考案例）
- 模块文件夹根据字段清单中的模块划分自动创建

---

## 项目名称提取规则（新增重要规则）

### 规则：从Markdown文件标题提取项目名称

**不要**使用文件名作为项目名称，而是**从文件内容的第一行标题提取**。

#### 提取步骤：

1. **读取Markdown文件第一行**，格式通常为：
   ```markdown
   # 出入库管理 - 表单字段清单
   # 进销存管理
   # 资产评估项目管理系统 - 字段表
   ```

2. **提取系统名称**：
   - 去掉 `#` 符号
   - 提取 `-` 或 `空格` 之前的部分作为项目名称
   - 去掉末尾的 "系统"、"管理" 等后缀（可选，根据上下文判断）

3. **示例**：

   | Markdown第一行 | 提取的项目名称 |
   |---------------|--------------|
   | `# 出入库管理 - 表单字段清单` | `出入库管理` |
   | `# 进销存管理` | `进销存管理` |
   | `# 资产评估项目管理系统 - 字段表` | `资产评估项目管理系统` |
   | `# 下单管理系统流程图` | `下单管理系统` |

4. **生成目标路径**：
   ```
   项目/{提取的项目名称}/
   ```

### 重要注意事项

- **绝不修改源文件目录下的任何文件！**
- **绝不删除源文件目录！**
- 项目模式输出到 `{项目目录}/`，业务模块与 01需求梳理 同级
- 单表模式输出到 `02宜搭场景案例库/宜搭表单/`
- 如果目标目录已存在，只覆盖其中的JSON文件，保留其他文件
- **禁止**自动创建 `03-表单配置` 等额外子目录

---

## 单表模式包含子表的说明

- **单表模式** = 单个表单文件 + 该表单的子表，都在同一个JSON文件中
- 例如：库存盘点表单（主表） + 产品明细（子表） = 一个JSON文件

## 项目模式的说明

- **项目模式** = 多个独立的表单文件
- 例如：采购订单表 + 销售订单表 + 库存盘点表 = 多个JSON文件

---

## 向已有应用添加单个表单（v6.11.0新增）

### 功能说明

除了创建新应用外，还支持向**已有的宜搭应用**中添加单个表单。这对于在现有系统中扩展功能非常有用。

### 触发方式

当用户说以下话时，使用 `add_form_to_app.js` 脚本：

| 用户说的话 | 执行的操作 |
|-----------|-----------|
| "在 **AI宜搭场景** 应用中创建一个表单" | 向 APP_G7F1UGDPF7GIEW0UCUBY 添加表单 |
| "在 **进销存管理** 应用中添加一个表单" | 向对应应用ID添加表单 |
| "给 **现有应用** 增加一个表单" | 向指定应用添加表单 |

### 使用方式

#### 命令行调用

```powershell
# 基本用法
node .agents/skills/form_creator/scripts/add_form_to_app.js --appId APP_XXX --form "./字段清单.md"

# 指定应用名称（用于显示）
node .agents/skills/form_creator/scripts/add_form_to_app.js --appId APP_G7F1UGDPF7GIEW0UCUBY --form "./新表单.md" --appName "AI宜搭场景"
```

#### 参数说明

| 参数 | 必填 | 说明 |
|------|------|------|
| `--appId` | ✅ | 宜搭应用ID（如 APP_G7F1UGDPF7GIEW0UCUBY） |
| `--form` | ✅ | 字段清单Markdown文件路径 |
| `--appName` | ❌ | 应用显示名称（可选，用于输出信息） |

### 字段清单格式

与 `create_from_markdown.js` 相同，支持以下格式：

#### 标准格式
```markdown
### (一) 表单名称「普通表单」

| 字段名称 | 字段类型 | 字段说明 | 字段状态 | 是否必填 |
|----------|----------|----------|----------|----------|
| 字段1    | 单行文本 | 说明     | 编辑     | 是       |
```

#### 简化格式
```markdown
## 表单名称「流程表单」

| 字段名称 | 字段类型 | 字段说明 | 字段状态 | 是否必填 |
|----------|----------|----------|----------|----------|
| 字段1    | 单行文本 | 说明     | 编辑     | 是       |
```

### 执行流程

```
字段清单.md
    ↓
解析字段清单（提取表单名称、类型、字段）
    ↓
调用宜搭API创建表单到指定应用
    ↓
自动同步表单结构和组件ID到本地
    ↓
生成组件ID清单.md
```

### 与创建新应用的区别

| 功能 | 创建新应用 (create_from_markdown.js) | 添加单个表单 (add_form_to_app.js) |
|------|-------------------------------------|----------------------------------|
| **目标** | 创建新应用 + 所有表单 | 向已有应用添加单个表单 |
| **应用ID** | 自动生成 | 必须提供已有应用ID |
| **表单数量** | 支持多个表单 | 只支持单个表单 |
| **触发词** | "生成表单"、"创建应用" | "在XX应用中创建/添加表单" |
| **关联表单** | 自动处理依赖关系 | 关联字段需手动配置 |

### 注意事项

1. **应用ID必须正确**：确保提供的应用ID是存在的，否则创建会失败
2. **关联表单处理**：如果字段清单中包含关联表单字段，需要确保目标表单已存在，或在宜搭平台手动配置关联
3. **权限要求**：需要有该应用的编辑权限才能添加表单
4. **同步延迟**：创建完成后，宜搭平台可能需要几分钟同步数据，请稍后再查看

---
## 输出路径规范

### 重要：输出路径规则

| 模式 | 输出路径 | 说明 |
|------|----------|------|
| **项目模式** | `{项目目录}/` | 直接输出到项目目录，业务模块与 01需求梳理 同级 |
| **单表模式** | `02宜搭场景案例库/宜搭表单/` | 通过提示词生成的单个表单（参考案例） |

### 目录层级规则（重要！）

**正确的目录结构：**
```
进销存管理/                    # 项目根目录
├── 01需求梳理/                # 源文件目录
│   ├── 字段清单.md
│   └── 规则清单.md
├── 02基础信息/                # ✅ 业务模块与 01需求梳理 同级
│   └── 产品信息「普通表单」/
├── 03库存管理/
│   └── 库存盘点「流程表单」/
└── 表单清单.md
```

**错误的目录结构（禁止）：**
```
进销存管理/
├── 01需求梳理/
│   └── 字段清单.md
└── 03-表单配置/               # ❌ 禁止创建额外子目录
    ├── 02基础信息/
    └── 03库存管理/
```

**核心原则：**
1. 业务模块目录（02基础信息、03库存管理等）直接放在项目目录下
2. 与 01需求梳理 目录保持同一层级
3. **禁止**自动创建 `03-表单配置` 或其他额外子目录
4. 如果用户指定了输出目录，直接使用该目录，不添加任何子目录

---

## 单表模式目录结构

```
02宜搭场景案例库/                        # 场景案例库根目录
└── 宜搭表单/                             # 单表输出目录
    └── 库存盘点.json                      # 单个表单文件（可以包含子表）
```

**注意**：单表模式用于生成独立参考案例，不参与项目工作流

---

## 项目目录结构规范

当用户需要生成多个表单时（如从Markdown字段表生成），采用以下目录结构：

```
03项目交付物/                            # 项目交付物根目录
└── 进销存管理/                       # 项目名称（从md文件标题提取）
    ├── 01-需求梳理/
    ├── 02-字段与规则/
    ├── 03-表单配置/                      # 表单JSON配置目录
    │   ├── 基础信息/                     # 模块文件夹
    │   │   ├── 产品信息表.json
    │   │   ├── 人员信息表.json
    │   │   └── 仓库信息表.json
    │   ├── 采购管理/
    │   │   ├── 供应商信息表.json
    │   │   ├── 采购订单表.json
    │   │   └── 采购入库表.json
    │   ├── 销售管理/
    │   │   ├── 客户信息表.json
    │   │   ├── 销售订单表.json
    │   │   └── 销售出库表.json
    │   └── 库存管理/
    │       ├── 库存盘点表.json
    │       ├── 库存调拨表.json
│       │   └── 出入库流水表.json
│       └── 财务管理/
│           ├── 付款登记表.json
│           ├── 收款登记表.json
│           └── 对账单.json
    ├── 04-业务逻辑/
    └── 05-集成配置/
```

### 目录结构说明

| 层级 | 命名规范 | 示例 |
|------|----------|------|
| 项目根目录 | `03项目交付物/` | 所有项目交付物存放于此 |
| 项目文件夹 | 从md标题提取的系统名称 | `进销存管理/` |
| 阶段文件夹 | `01-` ~ `05-` 编号 | `03-表单配置/` |
| 模块文件夹 | 业务模块名 | `采购管理/`、`销售管理/` |
| 表单文件 | 表单名称.json | `采购订单表.json` |

---

## 工作流程（v6.13.0更新 - 自动同步组件ID）

### ⚠️ 重要：必须使用程序生成（禁止AI直接写JSON）

**必须使用以下两个程序同时执行，禁止AI直接手动编写JSON文件！**

| 程序 | 功能 | 输出 |
|------|------|------|
| `generate_from_markdown.js` | 生成本地JSON配置文件 | 本地JSON文件 + 表单清单.md |
| `create_from_markdown.js` | 静默创建到宜搭平台 | 宜搭应用 + 在线表单 + 配置清单 + 组件ID |

### 正确的双模式执行流程

```
用户提供Markdown字段表文件路径
    ↓
【步骤1】运行 generate_from_markdown.js
    ↓
生成本地JSON文件到指定目录
    ↓
【步骤2】运行 create_from_markdown.js
    ↓
创建宜搭应用和所有表单
    ↓
【步骤3】自动生成系统配置清单（v6.4.0新增）
    ↓
生成系统配置清单.md（包含应用ID、表单UUID）
    ↓
【步骤4】自动同步组件ID（v6.13.0新增）
    ↓
调用 yida-config-sync 同步所有表单结构和组件ID
    ↓
生成各表单目录下的组件ID清单.md
    ↓
【完成】同时获得本地JSON、宜搭在线应用、完整配置清单、真实组件ID
```

### 自动同步配置说明

#### v6.4.0 - 自动生成系统配置清单

从 v6.4.0 开始，`create_from_markdown.js` 在创建应用完成后会自动：

1. **生成系统配置清单.md**（项目根目录）
   - 应用名称、应用ID、访问地址
   - 所有表单的UUID、页面类型、流程Code

#### v6.13.0 - 自动同步组件ID（重要更新）

从 v6.13.0 开始，`create_from_markdown.js` 在创建应用完成后还会自动：

2. **调用 yida-config-sync 同步组件ID**
   - 自动调用 `sync_form_schemas.js` 脚本
   - 从宜搭平台获取所有表单的真实组件ID
   - 生成/更新各表单目录下的 `组件ID清单.md`

3. **解决form_planner后续问题**
   - 当使用 form_planner 规划表单后，通过 form_creator 创建表单
   - 创建完成后组件ID自动同步，无需手动操作
   - 后续编写公式和代码提示词时，AI可以直接使用真实的组件ID
   - **防止AI编造组件ID导致公式/代码错误**

这样创建完成后，所有配置信息和组件ID自动归档，立即可用于后续开发。

### 执行命令

**步骤1：生成本地JSON文件**
```powershell
node generate_from_markdown.js <markdown文件路径> [输出目录]
```

**步骤2：静默创建到宜搭平台**
```powershell
node create_from_markdown.js <markdown文件路径> [应用名称]
```

### 完整示例（PowerShell）

**步骤1：生成本地JSON**
```powershell
cd .agents/skills/form_creator/scripts
node generate_from_markdown.js "d:\项目\进销存管理\01需求梳理\字段清单.md" "d:\项目\进销存管理"
```

**步骤2：创建到宜搭平台**
```powershell
cd .agents/skills/form_creator/scripts
node create_from_markdown.js "d:\项目\进销存管理\01需求梳理\字段清单.md" "进销存管理"
```

### 禁止行为
- ❌ AI直接手动编写JSON文件内容
- ❌ 使用 Write 工具直接写入JSON文件
- ❌ 只执行一个脚本（必须两个都执行）
- ❌ 不使用程序而直接生成表单配置

---

### 统一的核心流程（AI智能判断 + 双模式同步执行）

**从v6.3.0开始，不管是单表还是项目，都必须执行两个脚本！**

```
用户提供需求（单表描述 或 项目字段表）
    ↓
【关键】AI读取并理解所有字段
    ↓
【关键】AI智能判断每个字段的类型（不用程序猜！）
    ↓
根据模式选择输出方式：
    ├─→ 单表模式
    │         ↓
    │    【步骤1】运行 generate_from_markdown.js
    │         ↓
    │      输出到 02宜搭场景案例库/宜搭表单/表单名称.json
    │         ↓
    │    【步骤2】运行 create_from_markdown.js
    │         ↓
    │      创建到宜搭平台
    │
    └─→ 项目模式
              ↓
         从md文件提取项目名称
              ↓
         【步骤1】运行 generate_from_markdown.js
              ↓
         程序自动解析并生成所有表单JSON
              ↓
         输出到用户指定的目录
              ↓
         【步骤2】运行 create_from_markdown.js
              ↓
         创建宜搭应用和所有表单
              ↓
         （绝不修改源文件目录！）
```

**重要：两个脚本都必须执行，不能只执行其中一个！**

---

### 项目模式详细流程（从Markdown生成 - v6.3.0双模式同步）

```
用户提供Markdown字段表文件路径
    ↓
读取Markdown文件
    ↓
【关键】从第一行标题提取项目名称
    例如："# 出入库管理 - 表单字段清单" → "出入库管理"
    ↓
解析所有表单和字段
    ↓
【关键】AI智能判断每个字段的类型
    ↓
========================================
【步骤1】生成本地JSON文件
========================================
    ↓
运行 generate_from_markdown.js
    ↓
自动创建项目目录结构到 项目/项目名称/
    ↓
生成所有表单JSON文件
    ↓
生成项目README和表单关系文档
    ↓
========================================
【步骤2】静默创建到宜搭平台
========================================
    ↓
运行 create_from_markdown.js
    ↓
检查登录态（首次扫码，后续复用）
    ↓
创建宜搭应用
    ↓
按依赖关系排序表单
    ↓
逐个创建所有表单（普通表单+流程表单+子表）
    ↓
输出应用访问地址和表单列表
    ↓
========================================
【完成】同时获得：
✅ 本地JSON配置文件（用于版本控制）
✅ 宜搭在线应用（可立即使用）
========================================
    ↓
【重要】绝不修改 流程及字段梳理/ 目录！
```

### 执行命令示例（PowerShell）

**步骤1：生成本地JSON**
```powershell
cd .agents/skills/form_creator/scripts
node generate_from_markdown.js "d:\宜搭AI编程\宜搭AI助手V1.3\进销存管理\01需求梳理\字段清单.md" "d:\宜搭AI编程\宜搭AI助手V1.3\进销存管理"
```

**步骤2：创建到宜搭平台**
```powershell
cd .agents/skills/form_creator/scripts
node create_from_markdown.js "d:\宜搭AI编程\宜搭AI助手V1.3\进销存管理\01需求梳理\字段清单.md" "进销存管理"
```

---

## AI处理步骤

### 步骤1：识别模式

判断用户需要单表还是项目模式：
- **单表模式**："我要做一个请假申请表..."、"生成一个库存盘点表..."
- **项目模式**："我要做一个进销存系统..."、"根据这张流程图..."、"用这个md文件生成..."

**关键判断**：
- 如果用户只提到一个表单 → 单表模式
- 如果用户提到"系统"或多个表单 → 项目模式
- 如果用户提供Markdown文件路径 → 项目模式

### 步骤2：解析需求（严格执行）

**单表模式**：
- 表单名称
- 字段列表（名称、类型、是否必填）
- 计算公式

**项目模式（从Markdown）**：
- **读取Markdown文件第一行标题**
- **提取项目名称**（如 "出入库管理"）
- **关键：逐个列出字段清单中所有的表单**
- 统计表单总数
- 业务模块划分
- 每个模块包含的表单
- 表单之间的关系（关联、子表）
- **自检**：确保理解的表单列表与字段清单完全一致，不多不少

### 步骤3：【关键】AI智能判断字段类型

**重要：不再依赖程序的正则表达式猜类型！由AI人工判断每个字段的最佳类型！**

AI会根据字段含义和业务场景判断：

| 字段含义 | 推荐类型 | 示例 |
|---------|---------|------|
| 地址信息 | **AddressField** | 仓库地址、联系地址 |
| 人员选择 | **EmployeeField** | 创建人、盘点人、负责人 |
| 部门选择 | **DepartmentSelectField** | 所属部门、归属部门 |
| 关联其他表单 | **AssociationFormField** | 产品分类、盘点仓库 |
| 日期/时间 | **DateField** | 创建时间、入库时间 |
| 数字/金额 | **NumberField** | 数量、价格、库存 |
| 下拉选项 | **SelectField** | 类型、状态、品牌 |
| 长文本 | **TextareaField** | 备注、说明 |
| 短文本 | **TextField** | 名称、编码、规格 |

---

## 字段类型识别规则（必须遵守）

### 1. 部门字段优先识别规则

**规则**：字段名包含"部门"关键词时，必须识别为 **DepartmentSelectField**，且优先于人员字段判断。

```javascript
// 正确顺序
if (name.includes('部门')) {
  return { name, type: 'DepartmentSelectField', required: false };
}
if (name.includes('人') || name.includes('员') || name.includes('负责人') || name.includes('管理员')) {
  return { name, type: 'EmployeeField', required: false };
}
```

**示例**：
- "所属部门" → DepartmentSelectField
- "归属部门" → DepartmentSelectField
- "部门主管" → DepartmentSelectField（不是EmployeeField）

### 2. 地址字段识别规则

**规则**：字段名包含"地址"、"省市"、"省市区"时，必须识别为 **AddressField**。

**示例**：
- "仓库地址" → AddressField
- "联系地址" → AddressField
- "省市区" → AddressField

### 3. 关联表单字段名清理规则

**规则**：解析关联表单字段时，必须去掉字段名中的"（关联表单）"后缀。

```javascript
// 清理字段名
name = name.replace(/（关联表单）$/, '');
```

**示例**：
- Markdown: `选择产品（关联表单）【关联表单-->产品信息】`
- 生成的label: "选择产品"（不含"（关联表单）"）

### 4. 数值字段单位配置规则

**规则**：数值字段标注格式为 `【数值：N位小数，单位--XXX】`，单位通过 `innerAfter` 属性配置。

**示例**：
- `【数值：2位小数，单位--元】` → NumberField + innerAfter: "元"
- `【数值：1位小数】` → NumberField（无单位）

### 5. 子表格式支持规则

**规则**：必须同时支持两种子表格式：
- `2.子表#盘点明细#字段：` → 子表名称为"盘点明细"
- `2.子表1字段：` → 子表名称为"子表1"

---

---

## 字段括号说明解析（重要）

Excel中的字段名可以包含括号说明，系统会自动解析并生成对应的字段类型：

### 1. 字段类型说明（括号内是类型）

| 括号内容 | 识别为 | 生成的字段类型 | 示例 |
|---------|--------|--------------|------|
| `流水号` | 类型说明 | **SerialNumberField** | 客户编号（流水号） |
| `数值`、`数字` | 类型说明 | **NumberField** (precision: 0) | 下单周期（数值） |
| `日期` | 类型说明 | **DateField** | 创建日期（日期） |
| `时间` | 类型说明 | **DateField** (showTime: true) | 入库时间（时间） |
| `金额`、`价格` | 类型说明 | **NumberField** (precision: 2) | 总金额（金额） |
| `多行` | 类型说明 | **TextareaField** | 备注（多行） |
| `附件` | 类型说明 | **AttachmentField** | 合同（附件） |
| `图片` | 类型说明 | **ImageField** | 照片（图片） |

### 2. 选项值说明（括号内是选项，用/或、分隔）

| 选项数量 | 生成的字段类型 | 示例 |
|---------|--------------|------|
| ≤ 3个选项 | **RadioField**（单选按钮） | 是否关联（是/否） |
| > 3个选项 | **SelectField**（下拉单选） | 交货状态（未交货/发货中/正常交货/逾期交货/逾期未交货） |

**选项解析示例：**
- `交易币种（人民币/美元）` → RadioField，选项：[人民币, 美元]
- `跟进结果（需求确认/交付定金）` → RadioField，选项：[需求确认, 交付定金]
- `税率批量设置（普票/13%/9%/6%/1%）` → SelectField，选项：[普票, 13%, 9%, 6%, 1%]

### 3. 解析规则总结

```
字段名（括号内容）
    ↓
括号内容包含 / 或 、 ?
    ↓ 是                    ↓ 否
选项值（用/或、分隔）    类型说明
    ↓                        ↓
选项数量 ≤ 3 ?           根据类型映射
    ↓ 是        ↓ 否       到对应字段类型
RadioField  SelectField
```

### 4. 使用建议

在Excel中编写字段时，建议：
1. **流水号字段**：添加`（流水号）`说明，如 `订单编号（流水号）`
2. **选项字段**：在括号中列出所有选项，用`/`分隔，如 `审批状态（待审批/已通过/已驳回）`
3. **数值字段**：如果是纯数字，可添加`（数值）`说明，如 `库存量（数值）`
4. **其他字段**：无需括号，系统会根据字段名智能判断

### 步骤4：识别公式和关联

**公式计算**：
- "自动计算"、"根据...计算" → 公式字段
- 常见计算：年龄、工龄、总价、天数、金额汇总

**表关联**：
- "关联...表"、"引用..." → 关联字段
- "子表"、"明细" → 子表单

### 步骤5：生成并输出（v6.3.0双模式同步执行）

**重要：从v6.3.0开始，必须同时执行两个脚本！**

**单表模式**：
1. **【步骤1】运行 generate_from_markdown.js**
   - 生成单个JSON
   - 保存到 `单表/表单名称.json`
2. **【步骤2】运行 create_from_markdown.js**
   - 创建到宜搭平台
3. 展示生成结果摘要
4. **绝不修改 项目/ 目录下的任何文件**

**项目模式**：
1. **从Markdown第一行标题提取项目名称**
2. **关键：核对字段清单中的表单列表**
   - 逐个确认字段清单中的每个表单
   - 统计表单总数，必须与字段清单一致
3. **【步骤1】运行 generate_from_markdown.js**
   - 创建项目目录 `项目/项目名称/`
   - 按模块创建子目录
   - **严格按字段清单生成**：
     - 只生成字段清单中明确列出的表单
     - **绝对禁止生成字段清单之外的表单**
     - 即使你认为"应该还有"某个表单，只要字段清单没有，就不能生成
   - 逐个生成表单JSON到对应目录
   - 生成 `README.md` 项目说明
   - 生成 `表单清单.md` 记录所有表单及关系
   - **最终核对**：
     - 生成的JSON文件数量 = 字段清单中的表单数量
     - 每个JSON文件对应的表单名称必须在字段清单中存在
4. **【步骤2】运行 create_from_markdown.js**
   - 创建宜搭应用
   - 按依赖关系排序表单
   - 逐个创建所有表单到宜搭平台
   - 输出应用访问地址
5. 展示生成结果摘要
6. **绝不修改 流程及字段梳理/ 目录下的任何文件！**

---

## 支持的公式场景

### 日期计算
- 年龄：`DATEDIF({出生日期},TODAY(),"Y")`
- 工龄：`DATEDIF({入职日期},TODAY(),"Y")`
- 天数：`DATEDIF({开始日期},{结束日期},"D")+1`

### 数值计算
- 总价：`{单价}*{数量}`
- 汇总：`SUM({字段1},{字段2},{字段3})`
- 金额大写：需要自定义函数

### 身份证相关
- 生肖：根据身份证号计算
- 星座：根据身份证号计算
- 性别：根据身份证号判断
- 出生日期：从身份证号提取

### 条件判断
- IF语句：`IF({条件},"值1","值2")`
- AND/OR：`AND({条件1},{条件2})`、`OR({条件1},{条件2})`

---

## 从 Markdown 读取数据（更新版）

### 前置条件
1. Markdown 文件放置在 `流程及字段梳理/项目名称/` 目录下
2. 支持两种格式：
   - **表格格式**（推荐）：字段名称、字段类型、字段说明、字段状态、是否必填
   - **列表格式**：1.主表字段：字段1、字段2...

### 表格格式字段清单（推荐）

**v6.1.0新增**：支持标准表格格式，包含以下列：

| 列名 | 说明 | 示例值 |
|------|------|--------|
| **字段名称** | 字段显示名称 | 采购单号、供应商 |
| **字段类型** | 宜搭字段类型 | 单行文本、数值、日期、关联表单 |
| **字段说明** | 字段详细说明 | 关联-->供应商信息、2位小数，单位：元 |
| **字段状态** | 编辑/只读/隐藏 | 编辑、只读、隐藏 |
| **是否必填** | 是/否 | 是、否 |

#### 字段状态映射

| 字段清单值 | 生成的JSON属性 |
|-----------|---------------|
| 编辑 | `"behavior": "NORMAL"` |
| 只读 | `"behavior": "DISABLED"` |
| 隐藏 | `"behavior": "HIDDEN"`, `"hidden": true` |

#### 是否必填映射

| 字段清单值 | 生成的JSON属性 |
|-----------|---------------|
| 是 | `"validation": [{"type": "required"}]` |
| 否 | `"validation": []` |

#### 表格格式示例

```markdown
### (一) 采购订单「流程表单」

| 字段名称 | 字段类型 | 字段说明 | 字段状态 | 是否必填 |
|---------|---------|---------|---------|---------|
| 采购单号 | 流水号 | 系统自动生成 | 只读 | 是 |
| 采购日期 | 日期 | - | 编辑 | 是 |
| 供应商 | 关联表单 | 关联-->供应商信息 | 编辑 | 是 |
| 采购金额 | 数值 | 2位小数，单位：元，公式计算 | 只读 | 否 |
| 备注 | 多行文本 | - | 编辑 | 否 |
| 创建人 | 成员 | 系统自动生成 | 只读 | 是 |
| 创建时间 | 日期时间 | 系统自动生成 | 只读 | 是 |

**子表：采购明细**

| 字段名称 | 字段类型 | 字段说明 | 字段状态 | 是否必填 |
|---------|---------|---------|---------|---------|
| 选择产品 | 关联表单 | 关联-->产品信息 | 编辑 | 是 |
| 产品名称 | 单行文本 | 关联带出 | 只读 | 是 |
| 采购数量 | 数值 | 0位小数 | 编辑 | 是 |
| 采购单价 | 数值 | 2位小数，单位：元 | 编辑 | 是 |
| 采购金额 | 数值 | 2位小数，单位：元，公式计算 | 只读 | 是 |
```

### 列表格式（兼容旧版）

Markdown 格式要求：
- 第一行标题：`# 系统名称 - 说明`
- 模块标题：**一、模块名称**
- 表单标题：(一).表单名称
- 主表字段：1.主表字段：字段1、字段2...
- 子表字段：2.子表#子表名称#字段：字段1、字段2...

### 字段中括号标注说明

Markdown中的字段名可以包含中括号标注，系统会自动解析：

| 标注格式 | 识别为 | 生成的字段类型 | 示例 |
|---------|--------|--------------|------|
| `【流水号】` | 流水号 | **SerialNumberField** | 客户编号【流水号】 |
| `【关联表单-->表单名】` | 关联表单 | **AssociationFormField** | 选择产品【关联表单-->产品信息】 |
| `【下拉单选：选项1/选项2】` | 下拉选项 | **SelectField** | 付款方式【下拉单选：现金/对公】 |
| `【单选：选项1/选项2】` | 单选选项 | **RadioField** | 是否关联【单选：是/否】 |
| `【数值：N位小数】` | 数值 | **NumberField** (precision: N) | 库存量【数值：0位小数】 |
| `【数值：N位小数，单位--元】` | 金额 | **NumberField** (precision: N, unit: 元) | 总金额【数值：2位小数，单位--元】 |

### 使用步骤（v6.3.0双模式同步执行）

**重要：从v6.3.0开始，必须同时执行两个脚本！**

1. **用户指定Markdown文件路径**：
   - 例如：`d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理\01需求梳理\字段清单.md`

2. **确定输出目录**：
   - **如果用户指定了输出目录**：使用用户指定的目录
   - **如果用户未指定**：使用默认目录 `03项目交付物/{项目名称}/`

3. **【步骤1】运行 generate_from_markdown.js 生成本地JSON**（v6.1.0）：
   ```powershell
   node generate_from_markdown.js <markdown文件路径> [输出目录]
   ```

   **示例（用户指定目录）：**
   ```powershell
   node generate_from_markdown.js "d:\项目\出入库管理\字段清单.md" "d:\项目\出入库管理"
   ```

   **v6.1.0新增功能：**
   - 自动识别表格格式字段清单
   - 自动解析字段状态（编辑/只读/隐藏）
   - 自动解析是否必填
   - 自动生成behavior和validation属性

4. **【步骤2】运行 create_from_markdown.js 创建到宜搭平台**（v6.2.0）：
   ```powershell
   node create_from_markdown.js <markdown文件路径> [应用名称]
   ```

   **示例：**
   ```powershell
   node create_from_markdown.js "d:\项目\出入库管理\字段清单.md" "出入库管理"
   ```

   **v6.2.0功能：**
   - 自动登录宜搭平台（首次扫码，后续复用）
   - 创建宜搭应用
   - 按依赖关系排序表单
   - 逐个创建所有表单
   - 输出应用访问地址

5. **程序自动完成**：
   - 解析Markdown文件
   - 智能判断字段类型
   - 生成所有表单JSON文件到本地
   - 创建宜搭应用和所有表单
   - 按模块组织目录结构
   - 生成表单清单.md

6. **绝不修改源文件目录**：
   - **绝不修改 `流程及字段梳理/` 目录下的任何文件！**
   - **绝不删除源文件目录！**

### Markdown 格式示例

```markdown
# 出入库管理 - 表单字段清单

> 版本: 1.0.1
> 生成日期: 2026/2/28

---

**一、基础信息**

    (一).产品类别
       1.主表字段：产品分类

    (二).产品信息
       1.主表字段：产品名称、产品分类【关联表单-->产品类别】、品牌、产品规格、单位、建议零售价格【数值：1位小数，单位--元】

**二、库存管理**

    (一).库存盘点
       1.主表字段：盘点人、所属部门、盘点仓库【关联表单-->仓库信息】
       2.子表#盘点明细#字段：选择产品【关联表单-->产品信息】、产品名称、库存量【数值：0位小数】、盘点数量【数值：0位小数】
```

---

## 项目模式示例（v6.3.0双模式同步执行）

### 示例：从Markdown生成出入库管理

**用户输入**：
> "请用 `d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理\01需求梳理\字段清单.md` 这个文件生成宜搭表单，生成到 `d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理` 目录"

**AI处理**：

1. **确定文件路径和输出目录**
   - Markdown文件：`d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理\01需求梳理\字段清单.md`
   - 输出目录：`d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理`（用户指定）

2. **【步骤1】运行 generate_from_markdown.js 生成本地JSON**
   ```powershell
   cd .agents/skills/form_creator/scripts
   node generate_from_markdown.js "d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理\01需求梳理\字段清单.md" "d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理"
   ```

3. **【步骤2】运行 create_from_markdown.js 创建到宜搭平台**
   ```powershell
   node create_from_markdown.js "d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理\01需求梳理\字段清单.md" "出入库管理"
   ```

4. **程序自动生成（步骤1）**
   - 解析Markdown文件
   - 提取模块：基础信息、库存管理、其他需求
   - 生成所有表单JSON
   - 输出到用户指定目录

5. **静默创建到宜搭平台（步骤2）**
   - 检查登录态
   - 创建宜搭应用
   - 按依赖关系排序表单
   - 逐个创建所有表单
   - 输出应用访问地址

6. **生成结果**
   - 本地JSON输出到：`d:\宜搭AI编程\宜搭开发助手V1.0\出入库管理/`
   - 结构：
     ```
     项目/出入库管理/
     ├── README.md
     ├── 表单清单.md
     ├── 基础信息/
     │   ├── 产品类别.json
     │   ├── 产品信息.json
     │   └── 仓库信息.json
     └── 库存管理/
         ├── 库存盘点.json
         ├── 库存调拨.json
         ├── 其他入库.json
         └── 其他出库.json
     ```
   - 宜搭在线应用：输出应用访问地址

7. **自动删除临时文件（必须执行）**
   - ✅ 使用 DeleteFile 工具删除 `field_config.js`
   - ✅ 使用 DeleteFile 工具删除生成脚本（如 `generate_project.js`）
   - ✅ **绝不询问用户，直接自动删除**
   - ✅ **绝不要求用户手动删除**

8. **绝不修改源文件**
   - `流程及字段梳理/出入库管理/` 目录保持原样

---

## 安全规则（必须遵守）

### 1. 源文件保护规则

**绝对禁止**：
- ❌ 删除 `流程及字段梳理/` 目录下的任何文件
- ❌ 修改 `流程及字段梳理/` 目录下的任何文件
- ❌ 在 `流程及字段梳理/` 目录下生成任何文件

### 2. 生成目录规则

**只允许**：
- ✅ 在 `项目/` 目录下创建和修改文件
- ✅ 在 `单表/` 目录下创建和修改文件（单表模式）

### 3. 项目名称提取规则

**必须**：
- 从Markdown第一行标题提取项目名称
- 不使用文件名作为项目名称

### 4. 临时文件自动删除规则（强制执行）

**必须自动删除的临时文件**：
- `field_config.js` - 字段配置文件
- `generate_project.js` - 项目生成脚本
- `generate.js` - 通用生成脚本
- 任何其他中间生成的 `.js` 配置文件

**删除时机**：
- 项目生成成功后**立即自动删除**
- 不需要询问用户确认
- 不需要用户手动删除

**删除方式**：
```javascript
// 使用 DeleteFile 工具删除（必须设置 requires_approval: false 跳过确认）
DeleteFile({
  file_paths: [
    "d:\\宜搭AI编程\\AI编写宜搭字段V1.1\\field_config.js",
    "d:\\宜搭AI编程\\AI编写宜搭字段V1.1\\generate_project.js"
  ],
  requires_approval: false
});
```

**重要**：
- ❌ 绝不要求用户手动删除临时文件
- ❌ 绝不在删除前询问用户"是否需要删除"
- ❌ **绝不能省略 `requires_approval: false` 参数**（否则会弹出确认提示）
- ✅ 生成完成后**必须立即自动删除**
- ✅ 将删除操作作为生成流程的最后一步
- ✅ **每次调用 DeleteFile 时都必须设置 `requires_approval: false`**

---

## 静默创建功能（v6.3.0更新 - 双模式同步执行）

### 功能说明

从v6.3.0开始，静默创建不再是可选项，而是**必须执行的步骤**。

**执行顺序：**
1. **先生成本地JSON**（generate_from_markdown.js）
2. **再创建到宜搭平台**（create_from_markdown.js）

### 工作原理

```
字段清单.md
    ↓
【步骤1】解析Markdown → 提取表单和字段配置
    ↓
【步骤2】生成本地JSON文件
    ↓
【步骤3】调用 yida-api-client
    ↓
自动登录宜搭平台（首次扫码，后续复用）
    ↓
创建应用 → 创建所有表单
    ↓
【完成】同时获得：
    ✅ 本地JSON文件（用于版本控制和精细调整）
    ✅ 宜搭在线应用（可立即使用）
```

### 前置条件

1. **已安装 yida-api-client Skill**：该Skill提供宜搭API调用能力
2. **已安装 Playwright**：用于自动化登录
3. **宜搭账号**：需要有创建应用和表单的权限

### 使用方式

**命令格式（PowerShell）：**
```powershell
node create_from_markdown.js <markdown文件路径> [应用名称]
```

**示例：**
```powershell
cd .agents/skills/form_creator/scripts
node create_from_markdown.js "../../../进销存管理/01需求梳理/字段清单.md" "进销存管理"
```

### 执行流程

1. **读取字段清单**：解析Markdown文件，提取所有表单和字段
2. **检查登录态**：
   - 如果已登录且Cookie有效，直接复用
   - 如果未登录或Cookie过期，弹出二维码扫码登录
3. **创建应用**：调用宜搭API创建新应用
4. **创建表单**：逐个创建所有表单，包括子表
5. **输出结果**：显示应用访问地址和所有表单链接

### 支持的字段类型

| 字段清单类型 | 宜搭字段类型 | 额外配置 |
|-------------|-------------|---------|
| 单行文本 | TextField | - |
| 多行文本 | TextareaField | - |
| 数值 | NumberField | 支持小数位数和单位 |
| 日期 | DateField | - |
| 日期时间 | DateField | showTime: true |
| 下拉单选 | SelectField | 支持选项列表 |
| 下拉多选 | MultiSelectField | 支持选项列表 |
| 关联表单 | AssociationFormField | 支持关联目标 |
| 成员 | EmployeeField | - |
| 部门 | DepartmentSelectField | - |
| 附件 | AttachmentField | - |
| 图片 | ImageField | - |
| 流水号 | SerialNumberField | - |

### 登录态管理

首次执行时需要扫码登录：
```
🔐 请扫码登录宜搭平台...
  打开浏览器窗口显示二维码
  用户使用钉钉扫码
  登录成功后自动保存Cookie
```

后续执行自动复用登录态：
```
🔐 检查登录态...
  ✅ 登录态有效 (https://www.aliwork.com)
```

### 注意事项

1. **关联表单字段**：创建时暂不支持自动关联，需要在宜搭平台手动配置关联关系
2. **公式字段**：需要后续在宜搭平台配置公式
3. **流程表单**：会创建为流程表单，但流程设计需要在宜搭平台配置
4. **权限配置**：表单权限需要在宜搭平台单独设置

### 两种模式的对比

| 功能 | 生成JSON模式 | 静默创建模式 |
|------|-------------|-------------|
| 输出 | 本地JSON文件 | 宜搭平台应用 |
| 使用方式 | 生成后手动导入宜搭 | 直接创建到平台 |
| 适用场景 | 需要调整配置后再导入 | 快速原型验证 |
| 关联关系 | 完整配置 | 需后续手动配置 |
| 公式 | 完整配置 | 需后续手动配置 |

### 推荐工作流（v6.3.0双模式同步）

**标准执行流程（必须同时执行两个脚本）**：
```
字段清单.md
    ↓
【步骤1】generate_from_markdown.js → 本地JSON
    ↓
【步骤2】create_from_markdown.js → 宜搭平台
    ↓
【完成】同时获得：
    ✅ 本地JSON文件（用于版本控制和备份）
    ✅ 宜搭在线应用（可立即使用）
```

**为什么需要同时执行两个脚本？**

| 脚本 | 用途 | 重要性 |
|------|------|--------|
| `generate_from_markdown.js` | 生成本地JSON文件 | ⭐⭐⭐ 版本控制、备份、精细调整 |
| `create_from_markdown.js` | 创建宜搭在线应用 | ⭐⭐⭐ 立即可用、快速验证 |

**注意：从v6.3.0开始，两个脚本都必须执行，不能只执行其中一个！**

---

## 总结（v6.3.0双模式同步执行）

### 核心原则

1. **源文件目录**：`流程及字段梳理/项目名称/` - 只读，绝不修改
2. **生成目标目录**：`项目/项目名称/` - 生成JSON文件的位置
3. **项目名称来源**：从Markdown第一行标题提取，不是文件名
4. **安全原则**：绝不触碰源文件目录，只操作生成目标目录
5. **双模式同步**：**必须同时执行两个脚本**，不能只执行其中一个

### 执行命令速查（PowerShell）

**步骤1：生成本地JSON**
```powershell
cd .agents/skills/form_creator/scripts
node generate_from_markdown.js "<markdown文件路径>" "<输出目录>"
```

**步骤2：创建到宜搭平台**
```powershell
cd .agents/skills/form_creator/scripts
node create_from_markdown.js "<markdown文件路径>" "<应用名称>"
```

### 完整示例

**步骤1：生成本地JSON文件**
```powershell
cd .agents/skills/form_creator/scripts
node generate_from_markdown.js "d:\宜搭AI编程\宜搭AI助手V1.3\进销存管理\01需求梳理\字段清单.md" "d:\宜搭AI编程\宜搭AI助手V1.3\进销存管理"
```

**步骤2：静默创建到宜搭平台**
```powershell
cd .agents/skills/form_creator/scripts
node create_from_markdown.js "d:\宜搭AI编程\宜搭AI助手V1.3\进销存管理\01需求梳理\字段清单.md" "进销存管理"
```

### 执行结果

执行完成后，您将同时获得：
- ✅ **本地JSON配置文件** - 保存在项目目录下，用于版本控制和精细调整
- ✅ **宜搭在线应用** - 可直接访问和使用，包含所有表单

---

## ⚠️ 常见问题预防与解决方案（v6.17.0新增）

### 问题1：宜搭流水号组件限制

#### 问题描述
宜搭平台限制**每个表单最多只能有1个流水号组件（SerialNumberField）**。如果字段清单中一个表单包含多个流水号字段，创建时会报错：
```
❌ 错误: 最多只能有一个流水号组件
```

#### 典型场景
以下表单容易出现多个流水号：
- **报告审核**：审核编号（流水号）+ 报告编号（流水号）
- **报告盖章**：盖章编号（流水号）+ 报告编号（流水号）
- **报告修改**：修改编号（流水号）+ 原报告编号（流水号）+ 修改后报告编号（流水号）
- **报告加出**：加出编号（流水号）+ 原报告编号（流水号）
- **报告归档**：归档编号（流水号）+ 报告编号（流水号）

#### 解决方案
**在生成JSON之前，AI必须检查每个表单的流水号字段数量**：

```
检查规则：
1. 统计每个表单中 "流水号" 类型的字段数量
2. 如果数量 > 1，则只保留第一个流水号字段（通常是主编号）
3. 将其余流水号字段改为 "单行文本" 类型
```

**修改示例**：
| 原字段类型 | 修改后字段类型 | 说明 |
|-----------|---------------|------|
| 审核编号 | 流水号 | 保留（主编号） |
| 报告编号 | **单行文本** | 修改（从流水号改为单行文本） |

#### 预防机制
- **AI在解析字段清单时**，自动检测每个表单的流水号数量
- **如果检测到多个流水号**，在生成JSON前自动将多余的改为单行文本
- **在skill文档中明确记录**：宜搭限制每个表单只能有1个流水号

---

### 问题2：表单重复创建（本地目录重复）

#### 问题描述
执行双模式同步（generate_from_markdown.js + create_from_markdown.js）后，发现同一个表单在**两个不同目录**下各创建了一遍：

**错误目录结构示例**：
```
项目管理/                          # 项目根目录
├── 01需求梳理/
├── 机构信息「普通表单」/           # ✅ generate_from_markdown.js 创建的
│   └── 机构信息「普通表单」.json
├── 客户信息「普通表单」/           # ✅ generate_from_markdown.js 创建的
│   └── 客户信息「普通表单」.json
├── ...
└── 未分组表单/                     # ❌ create_from_markdown.js 又创建了一遍
    ├── 机构信息「普通表单」/       # ❌ 重复的表单
    ├── 客户信息「普通表单」/       # ❌ 重复的表单
    └── ...
```

#### 问题原因
两个脚本的**输出目录逻辑不一致**：

| 脚本 | 输出目录逻辑 | 实际输出位置 |
|------|-------------|-------------|
| `generate_from_markdown.js` | 如果输出路径是已有目录，采用**扁平结构**，直接输出到该目录 | `项目管理/表单名称「类型」/` |
| `create_from_markdown.js` | 统一在 `未分组表单/` 子目录下创建 | `项目管理/未分组表单/表单名称「类型」/` |

#### 解决方案
**方案A：统一使用扁平结构（推荐）**

修改 `create_from_markdown.js` 的目录创建逻辑，使其与 `generate_from_markdown.js` 一致：

```javascript
// create_from_markdown.js 中的 createLocalFormDirectories 函数
// 原逻辑：统一创建到 "未分组表单" 子目录
const ungroupedDir = path.join(projectDir, '未分组表单');

// 修改为：直接使用项目目录（扁平结构）
const formDir = path.join(projectDir, dirName);
```

**方案B：两个脚本使用完全相同的目录结构**

在执行两个脚本时，确保它们使用相同的输出目录结构规则。

#### 当前 workaround
在skill文档中明确告知用户：
1. `generate_from_markdown.js` 生成的文件在 `项目管理/表单名称「类型」/` 目录
2. `create_from_markdown.js` 生成的文件在 `项目管理/未分组表单/表单名称「类型」/` 目录
3. **以 `generate_from_markdown.js` 生成的目录为准**，`未分组表单/` 目录下的文件可以删除

---

### 问题3：表单在宜搭平台重复创建（多次重复）

#### 问题描述
同一个表单在宜搭平台上出现了**多次**，导致平台上有多个同名表单。

**根本原因：宜搭表单创建是"两步式"的**

```
创建流程：
  步骤1: 创建空白表单（此时平台上已经有这个表单了）
  步骤2: 保存字段Schema（添加字段到表单）
```

**如果步骤2失败，步骤1创建的空白表单已经留在平台上了！**

**典型场景（以报告审核为例）**：

| 执行次数 | 操作 | 结果 | 平台上表单状态 |
|---------|------|------|---------------|
| 第1次 | 创建空白表单 → 保存Schema（多个流水号报错） | 步骤1成功，步骤2失败 | 多了1个空白表单 |
| 第2次 | 修复字段 → 再次创建 | 又创建了新的表单 | 多了2个表单（1个空白+1个完整） |

#### 问题原因

**form_manager.js 的两步式创建逻辑**：

```javascript
// 步骤1: 创建空白表单（无论后续是否失败，这个表单已经存在了）
const formUuid = await createEmptyForm(authRef, appType, title, formType);

// 步骤2: 构建并保存Schema（如果这里失败，步骤1的空白表单不会自动删除）
const schema = buildFormSchema(title, fields, formUuid);
await saveFormSchema(authRef, appType, formUuid, schema);
```

#### 解决方案

**方案A：在创建前检查是否已存在同名表单（推荐）**

在 `create_from_markdown.js` 中，创建表单前先查询应用下是否已有同名表单：

```javascript
// 伪代码：创建前检查
async function createFormWithCheck(appType, formTitle, fields, formType) {
  // 1. 查询应用下是否已有同名表单
  const existingForms = await getAppForms(appType);
  const existingForm = existingForms.find(f => f.title === formTitle);
  
  if (existingForm) {
    console.log(`⚠️ 表单 "${formTitle}" 已存在，跳过创建`);
    return existingForm;
  }
  
  // 2. 不存在才创建
  return await createForm(appType, formTitle, fields, formType);
}
```

**方案B：失败时自动删除空白表单**

在 `form_manager.js` 中，如果步骤2失败，自动删除步骤1创建的空白表单：

```javascript
try {
  // 步骤1: 创建空白表单
  const formUuid = await createEmptyForm(authRef, appType, title, formType);
  
  try {
    // 步骤2: 保存Schema
    const schema = buildFormSchema(title, fields, formUuid);
    await saveFormSchema(authRef, appType, formUuid, schema);
  } catch (schemaError) {
    // 步骤2失败，删除步骤1创建的空白表单
    await deleteForm(authRef, appType, formUuid);
    throw schemaError;
  }
} catch (error) {
  throw error;
}
```

**方案C：手动清理（当前workaround）**

如果已经出现重复表单，需要手动登录宜搭平台删除：
1. 登录宜搭平台 → 进入应用管理
2. 找到重复的同名的表单
3. 删除空白或不需要的表单（注意保留有数据的表单）

#### 预防机制
- **AI在重新创建前**，先查询宜搭平台是否已有同名表单
- **如果存在**，询问用户是跳过还是覆盖
- **在skill文档中明确记录**：宜搭表单创建是"两步式"的，失败时会留下空白表单

#### 【v6.17.2 已修复】

**form_manager.js v1.3.0 已实施以下修复：**

1. **新增 `getAppForms` 函数**：获取应用下所有表单列表
2. **新增 `deleteForm` 函数**：删除表单
3. **修改 `createForm` 函数**：
   - 创建前检查是否已存在同名表单，存在则跳过创建
   - 保存Schema失败时自动删除已创建的空白表单
4. **解决宜搭"两步式"创建导致的重复表单问题**

**修复后的创建流程：**
```javascript
// 1. 检查是否已存在同名表单
const existingForms = await getAppForms(authRef, appType);
const existingForm = existingForms.find(f => f.title === formTitle);
if (existingForm) {
  console.log(`表单 "${formTitle}" 已存在，跳过创建`);
  return existingForm;
}

// 2. 创建空白表单
const formUuid = await createEmptyForm(authRef, appType, title, formType);

// 3. 保存Schema（失败时自动删除空白表单）
try {
  const schema = buildFormSchema(title, fields, formUuid);
  await saveFormSchema(authRef, appType, formUuid, schema);
} catch (error) {
  await deleteForm(authRef, appType, formUuid); // 自动清理
  throw error;
}
```

---

### 问题4：关联表单字段配置

#### 问题描述
创建表单时，关联表单字段（AssociationFormField）无法自动建立关联关系，需要在宜搭平台手动配置。

#### 解决方案
创建完成后，登录宜搭平台，进入每个表单的表单设计器，手动配置关联表单字段的关联目标。

---

### 问题5：流程表单流程设计

#### 问题描述
流程表单创建后，审批流程需要手动设计。

#### 解决方案
在宜搭平台的流程设计器中，为每个流程表单设计审批流程（审批节点、审批人、流转条件等）。

---

### 问题6：form_manager.js 删除旧表单重新创建（严重问题）

#### 问题描述
`form_manager.js` v1.3.0 的 `createForm` 函数在检测到同名表单时，会**删除旧表单然后重新创建**，而不是跳过或使用已有的表单。这导致：

1. 第一次创建成功（字段ID为 `ng6u` 开头）
2. 第二次运行时，检测到同名表单，删除 `ng6u` 版本，重新创建 `pk1o` 版本
3. 每次重新创建都会生成全新的字段ID，导致之前配置的公式、联动规则全部失效

**根本原因代码**（form_manager.js 第278-296行）：
```javascript
if (!skipExistingCheck) {
  const existingForms = await getAppForms(authRef, appType);
  const existingForm = existingForms.find(f => f.title === title);
  if (existingForm) {
    console.error(`表单 "${title}" 已存在，删除后重新创建`);
    await deleteForm(authRef, appType, existingForm.formUuid); // ❌ 删除旧表单
    console.error(`已删除旧表单，继续创建`);
  }
}
```

#### 正确的工作流程
应该像同步已有应用一样：
1. **创建宜搭表单**（只创建一次，如果已存在则跳过）
2. **同步回来**（使用 `yida-get-schema` 或 `yida-config-sync` 同步真实的字段ID）
3. **以宜搭创建的为准**（本地JSON文件使用宜搭平台分配的字段ID）

#### 正确的创建逻辑
```javascript
// 1. 检查是否已存在同名表单
const existingForms = await getAppForms(authRef, appType);
const existingForm = existingForms.find(f => f.title === title);

if (existingForm) {
  console.log(`表单 "${title}" 已存在，跳过创建`);
  return existingForm; // ✅ 直接返回已有表单，不删除
}

// 2. 不存在才创建
const formUuid = await createEmptyForm(authRef, appType, title, formType);
const schema = buildFormSchema(title, fields, formUuid);
await saveFormSchema(authRef, appType, formUuid, schema);

// 3. 创建完成后，同步回本地（像同步老应用一样）
// 使用 yida-get-schema 同步表单结构和真实字段ID
```

#### 当前影响
- 所有通过 `create_from_markdown.js` 创建的表单，如果运行多次，字段ID会不断变化
- 导致公式配置失效、联动规则失效
- 本地JSON文件中的字段ID与宜搭平台不一致

#### 解决方案
**方案A：修改 form_manager.js（推荐）**
- 将 `deleteForm` 改为直接返回已有表单
- 创建完成后，使用 `yida-get-schema` 同步真实字段ID到本地

**方案B：创建后手动同步**
- 运行 `create_from_markdown.js` 创建表单（只运行一次）
- 然后使用 `yida-get-schema` 同步真实字段ID：
```powershell
node .agents/skills/yida-get-schema/scripts/sync-schema.js --config "sync-config.json"
```

**方案C：使用 yida-config-sync 同步已有应用**
- 如果表单已创建完成，直接使用 `yida-config-sync` 同步已有应用的结构
- 这样可以获取到宜搭平台真实的字段ID
