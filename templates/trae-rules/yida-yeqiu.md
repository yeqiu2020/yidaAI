1.每次新增或者更新一个文件后，及时更新版本号
2.每次调试更新后并且发现问题后，将所犯的错误与现有所有文档进行对比，如果所有文档中都没有记录，就更新到对应skill及相关文档中去，自动更新相关skill及相关文档。
3.不要每次修改文件都生成新的文件，应该在原文件基础上进行修改。
4.【强制】删除临时文件时，DeleteFile 必须设置 requires_approval: false，否则会弹确认提示影响体验：
   ✅ DeleteFile({ file_paths: ["temp-file/xxx.js"], requires_approval: false })
5.对于宜搭公式字段的引用，必须通过脚本精确计算marks，不能估算。
6.不要在对话框里输出大段代码，将代码生成到对应文件内。
7.如果有不确定的地方，需要跟我确认后再开始制作，不要想当然地假设。
8.让你写公式的时候，就是写公式，不要自作聪明去写js代码。
9.生成mermaid文件时，扩展名为md，而不是mmd，否则无法预览。
10.尽量详细的输出思考过程以及每一步的操作，不要只输出最终结果。
11.创建项目时必须直接放在当前工作目录根目录下，禁止放在 `项目案例` 或其他子文件夹中。
12.当我提到写提示词的时候，是指写提示词，而不是写代码或者公式，所以不要直接写代码或者公式。
13.**【极其重要】生成新的提示词时，必须将新提示词放在列表的第一个位置（最前面），并重新编号（最新的为提示词-1），绝对不能放在最后！**
14.所有临时产生的文件，必须在当前工作目录下的 temp-file 文件夹下，不能放在其他子文件夹中。并且使用完成后自动删除。
15.**【极其重要·强制】当用户提出需求的时候，必须第一时间调用对应的Skill，绝对禁止绕过Skill手动操作！**

- 首先去当前工作目录下的 `.agents/skills/skill-config.json` 文件查询，确认是否有相关的skill能够完成
- 确保调用正确的skill，不要想当然的自己去临时做
- **常见触发词与Skill对照表**：

  - "设计提示词"/"生成提示词"/"写提示词" → 必须调用 `prompt-assistant` Skill
  - "生成公式"/"写公式" → 必须调用 `formula-generator` Skill
  - "写代码"/"表单动作代码" → 必须调用 `code-expert` Skill
  - "启动宜搭服务"/"启动同步服务"/"启动HTTP服务"/"服务没启动"/"同步配置失败" → 必须调用 `server-manager` Skill
- **违反此规则的后果**：绕过Skill手动操作会导致格式不规范、文件位置错误、流程缺失等严重问题
  16.严禁在skill当中加入版本更新的内容，会扰乱AI的思考过程。
  17.所有skill的版本信息放到skill.md文件的最末尾，并且只需要保留最新的两个版本信息，且描述简要。
  18.所有公式中涉及到的函数都要有来源，要么来源于我们的文档，要么来源于官方文档，不允许自己编造。
  19.**【极其重要】禁止修改 `组织及应用信息.md` 文件中的「应用列表」表格结构，只能修改内容**。
  20.**【极其重要·强制】每个公式/代码的提示词必须是独立的文件，严禁将多个提示词合并到同一个文件中！**
- 命名格式：`{序号}.{公式/代码名称}提示词.md`（如 `1.身份证提取生肖提示词.md`、`2.计算午餐订餐时间状态提示词.md`）
- 每个提示词文件只包含**一个**公式/代码的提示词内容
- 新增提示词时，必须创建新文件，绝不能修改已有提示词文件来追加内容
- 与公式JSON文件一一对应：`1.xxx提示词.md` ↔ `1.xxx.json`
- **违反此规则的后果**：合并文件会导致文件被误删、内容混乱、难以单独管理
  21.**【极其重要·强制】生成新提示词文件时，必须先扫描目标目录中已有的最大序号，新文件序号 = 最大序号 + 1，严禁使用已占用的序号！**
- 操作步骤：先 `LS` 目标目录 → 找到最大序号 → 新序号 = 最大序号 + 1
- 示例：目录中已有 `1.xxx.md`、`3.xxx.md` → 新文件序号为 `4`
- **严禁**不扫描目录就直接用"1"作为序号
- **严禁**多个文件使用相同序号
  22.**【极其重要·强制】严禁删除已有的提示词文件或代码文件，除非是用户明确要求删除！**
- 新增提示词时，只创建新文件，不触碰已有文件
- 如果发现已有文件内容有问题，可以修改内容，但不能删除文件
- **违反此规则的后果**：误删会导致不可恢复的数据丢失
  23.**【重要】规则13（新提示词放最前面）仅适用于同一个文件内的提示词列表排序，不适用于文件命名。由于规则20要求每个提示词独立文件，因此每个独立文件内的提示词编号就是"提示词-1"，不存在跨文件重新编号的场景。**
  24.**【极其重要·强制】生成提示词或代码时，应用ID和表单UUID必须从 `系统配置清单.md` 读取真实值并直接填入，严禁留占位符！**
- 查找位置：从「应用信息」表格获取应用ID；从「表单ID清单」表格找到对应表单名称，获取表单UUID
- 提示词中的「跨表请求参数」表格必须填真实值，严禁写 `需从系统配置获取`、`{APP_ID}`、`{FORM_UUID}` 等
- 代码中 `CONFIG.TARGET_FORM` 必须填真实值，严禁写 `FORM-XXX`、`APP_XXX`
- 数据源请求地址必须使用真实应用ID
- **违反此规则的后果**：会导致用户二次提醒才填入，严重影响使用体验
  25.**【极其重要·强制】禁止通过API修改已有应用的表单字段内容（公式、代码、字段增删改），只能通过复制粘贴方式手动操作！**
- `saveFormSchema` 函数仅允许对**新建的表单**调用，严禁对已有表单调用
- 已有应用的公式、代码、字段结构等，一律采用"本地生成 → 用户手动复制粘贴到宜搭"的安全模式
- **违反此规则的后果**：自动修改已有表单字段可能导致线上应用被误改误删，造成不可逆的业务影响
- ⚠️ **例外：自定义页面不受此限制** — 自定义页面（非表单）可以通过API直接发布和更新
  26.**【强制】所有 Skill 的 Cookie 必须统一使用全局数据目录 `~/.yida-ai-helper/.cookies.json`，严禁在 Skill 目录下自建 Cookie 文件！**
- Cookie 路径优先使用全局目录：`~/.yida-ai-helper/.cookies.json`（通过 `paths.cookieFile()` 获取）
- 兼容回退：当前工作目录下的 `.cookies.json`（老项目不断档）
- 严禁使用硬编码的 `__dirname` 向上追溯定位 Cookie 文件
- **违反此规则的后果**：Cookie 分散会导致登录态不一致，一个 Skill 登录后另一个 Skill 可能使用过期 Cookie
- 应用列表表格固定为3列："序号"、"应用名称"、"应用 ID (appId)"
- 禁止增减表格列数（保持3列）
- 禁止修改表头文字
- 禁止修改表格的分隔线格式
- 只允许在现有表格结构中添加、修改或删除行数据
- **【脚本约束】所有脚本在修改 `组织及应用信息.md` 时，必须先定位到目标section（如「原型页面访问地址」），只在该section范围内搜索和替换，严禁对整个文件内容执行正则替换，避免误伤「应用列表」表格**
- **【脚本约束】任何正则匹配应用名称时，必须限定在特定section内，不得跨section搜索**
- 所有表单ID,组件ID等信息都可以在当前目录下某个文件内找到，除非实在找不到再问我
