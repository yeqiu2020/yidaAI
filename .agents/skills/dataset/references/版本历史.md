# dataset skill 版本历史

> 本文件为 dataset skill 的完整版本记录，正文只保留当前版本号一行。

v2.5.0 (2026-07-10):
- 🔴 **新增3条硬规则(9-11)**：创建前检查已有数据集、禁止创建重复视图表版本、创建后验证并标记状态
- 🔴 **第1步新增检查已有数据集流程**：读取`数据集配置.md`，匹配主表+关联表+关联字段，已有可用则直接复用
- ✅ **根因修复**：解决历史中同一配置创建6个视图表版本(v2/v3/v4/v5/三方联合/三方联合v2)的重复堆积问题

v2.4.0 (2026-07-09):
- 🔧 **通用化改造**：`create-view-table.js` 改为**配置驱动**，彻底去除硬编码
- ✅ **支持任意数量关联表** + **关联表字段可输出**
- ✅ **新增 `--dry-run`**：只解析配置并构建字段，不发请求
