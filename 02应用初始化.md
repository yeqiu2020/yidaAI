# 应用初始化流程

## 步骤一：启动宜搭服务

```
启动宜搭服务
```

> AI 会调用 `server-manager` skill 完成以下操作：
> 1. 启动 HTTP 静态服务（端口 8080）和同步配置服务（端口 3457）
> 2. 服务在后台独立运行（关闭终端不影响服务）
> 3. **AI 自动通过 OpenPreview 打开本地操作页面** `http://127.0.0.1:8080/本地操作页面/index.html`
>
> **不管是否已初始化组织信息**，本步骤都会打开页面：
> - 未初始化时，页面顶部组织名称位置会显示醒目的 **「🔧 初始化组织信息」** 按钮
> - 已初始化时，直接显示组织名称和应用列表
>
> **多组织并存模式**：服务以项目父目录为根，所有兄弟项目共享端口。URL 中的项目目录名（如当前工作目录名）由 `server_manager.js` 自动提取。
>
> **服务已在运行怎么办**：AI 会检测到服务已运行，直接 OpenPreview 打开页面，不会重复启动。
>
> **如何停止服务**：让 AI 执行「停止宜搭服务」或运行 `node ".agents\skills\server-manager\scripts\server_manager.js" stop`

## 步骤二：在页面点击「初始化组织信息」按钮（仅首次需要）

**首次访问**时，组织名称位置会显示一个带脉冲动画的 **「🔧 初始化组织信息」** 按钮。

> **无需 AI 介入**：点击按钮后，前端通过 SSE 流式接口直接调用后端 `org-init/scripts/init-org.js` 脚本，
> 脚本会自动完成「登录宜搭 → 抓取组织信息 → 更新 `组织及应用信息.md`」整个流程。
>
> - 弹窗会实时展示每一步执行日志（登录、抓取、保存等）
> - 首次初始化时若 `组织及应用信息.md` 不存在会自动创建
> - 初始化成功后页面自动重载，组织名称显示，按钮自动消失（一次性引导）
> - 全程无需在 AI 对话框输入任何指令

## 步骤三：日常使用「刷新应用信息」按钮

初始化完成后，应用管理 Tab 顶部会显示 **「🔄 刷新应用信息」** 按钮，用于后续同步宜搭端最新应用列表。

> **何时需要点击「刷新应用信息」**：
> - 宜搭端新增/删除了应用
> - 应用 ID 变化
> - 切换了宜搭组织
> - Cookie 过期导致同步失败（此时建议先点「🔄 刷新登录态」再点本按钮）

## 两个按钮的区别

| 按钮 | 位置 | 显示时机 | 语义 |
|------|------|---------|------|
| 🔧 初始化组织信息 | 组织名称位置（页面顶部） | 仅当未初始化时显示，初始化成功后自动消失 | 一次性引导，首次登录并获取组织信息+应用列表 |
| 🔄 刷新应用信息 | 应用管理 Tab 顶部 | 始终显示（已有组织数据后） | 日常同步宜搭端最新应用列表 |

> 两个按钮底层调用同一接口 `/refresh-org-apps`（都会更新组织信息+应用列表），仅 UI 文案和触发场景不同。
> 设计原则：**「初始化」是一次性引导动作，完成后不应再出现在显眼位置**；**「刷新」是常规同步动作，长期保留**。

***

*版本: v3.5.0 | 更新: 2026-08-17*
