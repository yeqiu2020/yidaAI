# form-to-prototype 版本记录

> 当前版本见 [../SKILL.md](../SKILL.md)。

| 版本 | 日期 | 更新内容 |
|-----|------|---------|
| 2.37.1 | 2026/08/17 | **【根因修复】模板字符串转义导致所有应用原型页面左侧菜单消失（全量重建事故）**。根因：`prototype_generator.js` 第 4120 行 app.js 模板用**反引号模板字符串**包裹，内部的 `\n` 被 JS 解析为**真实换行符**写入产物文件，生成的 app.js 出现跨行单引号字符串（`SyntaxError: Invalid or unexpected token`），`renderIndexMenu`/`renderMenu` 等全部未定义，左侧菜单（sidebar #menuItems）渲染为空。`GENERATOR_VERSION` 2.36.0 → 2.37.0 触发全量重建，该 bug 一次性波及所有应用。**修复**：1) `\n` → `\\n`（在模板字符串中输出字面量）；2) `validateGeneratedJs` 从"try-catch 只告警不阻断"改为"写入前校验，语法错误直接 throw 中止"；3) 递增 `GENERATOR_VERSION` 2.37.0 → 2.37.1 触发全量重建，所有应用用修复后模板重新生成。**教训**：详见「硬规则.md」第 3 条（模板转义）、第 4 条（自检阻断）、第 5 条（版本递增前干跑） |
| 2.37.0 | 2026/08/16 | **【根因修复】同步表单字段报 "signal timed out"**：`syncForm` catch 块新增 `LOGIN_REQUIRED` 检测——当后端 `sync_single_form.js` 检测到 Cookie 失效返回 `LOGIN_REQUIRED:` 前缀错误时，前端提示用户到「本地操作页面」点"刷新登录"按钮，而非显示模糊的 "signal timed out" 超时错误。配合 `sync_single_form.js` 改动（移除 Playwright 登录验证，改纯 Cookie 读取，同步耗时 ~15秒→~0.5秒）。`GENERATOR_VERSION` 2.36.0 → 2.37.0（触发全量重建，使新 syncForm 错误提示覆盖所有应用） |
| 2.36.0 | 2026/08/06 | **【修复】规则清单解析器支持两种格式**：1) `parseRulesData` 自动检测规则清单格式——类型中心格式（`## 一、大类` → `### 1. 子类` → 表格）和表单中心格式（`### 1. 表单名` → `#### ① 分类` → 表格），解决 excel-to-form 生成的类型中心格式规则清单在 manifest.html 中显示为空的问题；2) `renderRulesPanel` 改为动态分类（`_catOrder`），不再硬编码5类；3) `buildUpdatedMd('rules')` 和编辑回调同步适配两种格式；4) 清理调试代码。`GENERATOR_VERSION` 2.35.0 → 2.36.0 |
| 2.35.0 | 2026/08/02 | **【增强】字段清单「新增行」自动带下拉默认值**：原来 `addFieldRow` 新增行所有单元格均为空字符串，导致新行下拉列（字段类型/字段状态/是否必填）为空、无默认选中项。修复：新增行时按表头列名自动填默认值——字段类型=「单行文本」、字段状态=「普通」、是否必填=「否」，其余列留空；保存回写 `字段清单.md` 时新行也带默认值。`GENERATOR_VERSION` 2.34.0 → 2.35.0（触发全量重建） |
| 2.34.0 | 2026/08/02 | **【UI 优化】「使用说明」折叠箭头与分组箭头统一**：`字段清单使用说明` 折叠区的箭头由 `▸/▾`（偏小）改为与下方分组标题箭头完全一致的 `▼`（`font-size:11px`、色 `#409eff`、`margin-right:6px`、默认收起旋转 -90° 显示 `▶`，展开恢复 `▼`），并给 summary 加 `padding:10px 16px` 使箭头与下方分组三角形**左对齐**。`GENERATOR_VERSION` 2.33.0 → 2.34.0（触发全量重建） |
| 2.33.0 | 2026/08/02 | **【UI 优化】字段清单「使用说明」折叠区去图标并对齐**：1) 移除「字段清单使用说明」前的 `📘` 书本图标；2) 新增 `.manifest-collapse-summary` 样式，去掉 `<summary>` 默认缩进和折叠 marker，改为 `▸/▾` 箭头指示，使折叠标题与下方工具栏左对齐。`GENERATOR_VERSION` 2.32.0 → 2.33.0（触发全量重建） |
| 2.32.0 | 2026/08/02 | **【UI 优化】「全部收起 / 全部展开」合并为单个切换按钮**：字段清单工具栏原先两个独立按钮，现合并为一个按钮 `manifestToggleAllBtn`，点击后通过全局状态 `manifestAllExpanded` 切换——展开态显示 `📕 全部收起`、收起态显示 `📖 全部展开`，图标+文字随状态变化；新增 `manifestToggleAll()` / `updateManifestToggleBtn()`。`GENERATOR_VERSION` 2.31.0 → 2.32.0（触发全量重建） |
| 2.31.0 | 2026/08/02 | **【关键修复】字段清单「全部收起 / 全部展开」按钮无效**。根因：`manifestCollapseAll()`/`manifestExpandAll()` 里对 `parseFieldsData()` 的返回结果直接调用 `d.forEach(...)`，但 `parseFieldsData()` 返回的是**对象** `{ groups, head, raw }`（不是数组），`d.forEach` 抛 `d.forEach is not a function` 导致函数中断，按钮无反应。**修复**：改为 `const groups = d.groups || []; groups.forEach(...)`。`GENERATOR_VERSION` 2.30.0 → 2.31.0（触发全量重建） |
| 2.30.0 | 2026/08/02 | **【增强】字段清单「字段类型」改为预设下拉选项**：生成清单页字段清单表格里，「字段类型」列由原先 contenteditable 文本改为**下拉选择**，选项值来自字段清单"可用字段类型"预设（单行文本、多行文本、数值、日期、单选、复选、下拉单选、下拉复选、关联表单、成员、部门、附件、图片、地址、流水号）；若当前字段值超出预设，自动追加为自定义选项以免丢失。选择后自动保存回写 `字段清单.md`。视觉上加浅紫底（`data-kind="fieldtype"`）。`GENERATOR_VERSION` 2.29.0 → 2.30.0（触发全量重建） |
| 2.29.0 | 2026/08/02 | **【增强】字段清单「数据标题」可编辑**：生成清单页字段清单 Tab 里，每个表单标题右侧的「数据标题」由原先**只读展示**改为**可编辑输入框**（虚线下划线样式，hover/focus 高亮），修改后自动写回 `dataTitle` 并 `autoSave('fields')` 回写 `字段清单.md`（`**数据标题：xxx**`）。点击输入框时 `stopPropagation()` 避免触发表单折叠。`GENERATOR_VERSION` 2.28.0 → 2.29.0（触发全量重建） |
| 2.28.0 | 2026/08/02 | **【修复】迁移表单到新分组后，字段清单里原分组仍残留该表单**。根因：`syncFieldsGroups()`（应用分组→字段清单分组归属联动）在重建字段清单分组时，**允许同一表单名同时出现在多个分组的"包含表单"里**（如用户把"客户信息"加入"基础信息"但没从"客户管理"删除，字段清单里两个分组都有客户信息），违背字段清单"每个表单唯一归属一个分组"的语义。**修复**：`syncFieldsGroups()` 增加 `seen` 去重集合——按应用分组的先后顺序遍历，**同一表单只归属到首次出现的分组**，后续分组自动跳过该表单，保证字段清单里每个表单唯一。`GENERATOR_VERSION` 2.27.0 → 2.28.0（触发全量重建） |
| 2.27.0 | 2026/08/02 | **【关键修复】保存「应用分组」后分组大量重复（30 个分组全部重复多次）**。根因：`buildUpdatedMd('groups')` 用正则 `/…(\| 序号 \| 分组名称 \| 包含表单 \|)([\s\S]*?)(?=^#{1,2} \|\n---\n\|$)/m` 局部替换表格，**正则里 `$` 在多行模式下匹配到"表头行行尾"，导致 `[\s\S]*?` 匹配空串，`replace` 只把表头替换成新表格，旧数据行全部残留堆积**，每次保存重复一份；且 Windows 生成的 md 用 `\r\n`（CRLF），`\n---\n` 分隔线匹配不到，使"完整重建"方案也失效。**修复**：`buildUpdatedMd('groups')` 改为**用"分组表头首次出现位置" `indexOf(anchor)` 切分**——表头之前为文件头、表头之后第一个 `##` 起为尾部说明，中间整段用新表格**完整重建替换**，兼容 `\r\n`/`\n` 混合换行，彻底去重；同时修复 `bindManifestEdits` 里损坏的 `groups` 编辑分支（原先在 input 事件里误写整段完整重建代码并 `return`，导致编辑分组单元格**永不触发 autoSave**），改为正确写回 `groupsRows` 并触发 `autoSave('groups')`。`.generator-version` 已更新为 2.27.0，存量重复的 `应用分组.md` 已手动清理恢复为正常 6 组。`GENERATOR_VERSION` 2.26.0 → 2.27.0（触发全量重建） |
| 2.26.0 | 2026/08/02 | **【UI 优化】生成清单页字段清单表格列宽微调**：字段名称 16%→12%、字段类型 12%、字段说明 31%→42%、字段状态/是否必填 11%→10%、操作列 70px→8%。`GENERATOR_VERSION` 2.25.0 → 2.26.0（触发全量重建） |
| 2.25.0 | 2026/08/02 | **【增强】生成清单页顶栏手动保存按钮 + 新增/删除行自动保存**：1) **顶栏右上角新增「💾 保存」按钮**（绿色，位于组织主页旁），点击 `saveCurrentManifest()` 按当前激活 Tab 调用 `saveManifestFile` 回写对应 md 源文件，方便用户手动主动触发保存；2) **修复新增/删除行不保存问题**——`addGroupRow`/`deleteGroupRow`/`addFieldRow`/`deleteFieldRow` 原先只更新内存数据+重渲染、**不触发 autoSave**，导致"新增行会保存、删除行 md 无变化"；现四个函数均在操作后调用 `autoSave(tab)` 自动回写（防抖 500ms）；3) 新增全局 `currentTab` 跟踪当前激活 Tab 供顶栏按钮使用。`GENERATOR_VERSION` 2.24.0 → 2.25.0（触发全量重建） |
| 2.24.0 | 2026/08/02 | **【UI 优化】生成清单页字段清单表格列宽与下拉美化**：1) 字段清单表格改用 `table-layout: fixed`，新增列宽 class（`manifest-col-name/type/desc/select`），**字段说明列收窄至 28%**、**字段状态/是否必填列加宽至 17%**；2) `renderFieldCell` 新增 `colClassFor()` 按表头识别列类型并加对应 class；3) **字段状态/是否必填下拉框美化**——圆角、柔和边框、自定义箭头图标、hover/focus 高亮、字段状态浅蓝底、是否必填浅绿底。`GENERATOR_VERSION` 2.23.0 → 2.24.0（触发全量重建） |
| 2.23.0 | 2026/08/02 | **【关键修复】模板字符串转义 bug 全面排查与产物 JS 语法自检机制**：1) 修复 `buildUpdatedMd('groups')` 的 `\n`/`\s`/`\S` 单反斜杠（生成 manifest.html JS 语法错误）；2) **彻查并修复 3 处遗漏的单反斜杠**：guide.html 1105/1155 行 Windows 路径 `\'`、`\01`（改为正斜杠 `/`，消除八进制转义与撇号闭合错误）、manifest 2000/2006 行 `\s`→`s`（正则失效）、js/app.js 3910 行 `\d`→`d`（file:// 目录名识别失效）；3) **新增产物 JS 语法自检** `validateGeneratedJs()`（vm.Script 只解析不执行），generatePrototype 与 generateStaticFiles 写文件后自动校验 index/list/form/guide/manifest.html、form-config.js、app.js，发现语法错误即告警，防止再产出坏页面。**注意：模板字符串内所有反斜杠序列必须写双反斜杠**。`GENERATOR_VERSION` 2.22.0 → 2.23.0（触发全量重建，修复所有受影响应用）
    - **文档固化（随项目分发，不依赖记忆）**：form-to-prototype SKILL.md 新增专属硬规则3（模板字符串反斜杠转义规则）与规则4（生成后必须确认自检通过）；通用硬规则.md 新增第7条（跨 skill 单一来源）。确保任何修改生成器模板的开发者都能看到转义规则 |
| 2.22.0 | 2026/08/02 | **【关键修复】应用分组编辑自动保存 + 刷新数据丢失 + 表格重复**：1) `bindManifestEdits` 所有 contenteditable 单元格（应用分组/字段清单/规则清单）编辑后自动 `autoSave(tab)`（防抖 500ms 回写 md），无需手动点保存按钮；2) manifest 页每次打开/刷新**静默从本地读取三个 md**（`refreshManifestData(true)`），保证编辑后刷新能看到最新已保存数据，同步服务不可用时静默回退内嵌快照不打扰；3) `buildUpdatedMd('groups')` 由"正则替换首个表格"改为**完整重建整个应用分组.md**（文件头 + 新表格 + 使用说明），修复旧表格残留导致的重复行；4) `initGroups` 过滤完全空的分组行，避免表格残留空行；5) `GENERATOR_VERSION` 2.21.0 → 2.22.0（触发全量重建） |
| 2.21.0 | 2026/08/02 | **【联动】应用分组与字段清单分组归属联动同步**：保存「应用分组」Tab 后，自动调用新增的 `syncFieldsGroups()`，按调整后的应用分组（`groupsRows`）重建字段清单的分组归属并回写 `字段清单.md`，保证两个 md 文件的表单归属一致；字段清单中存在但应用分组未引用的表单自动归入「未分组」分组，避免数据丢失；保存后刷新字段清单 Tab 显示。`GENERATOR_VERSION` 2.20.0 → 2.21.0（触发全量重建） |
| 2.20.0 | 2026/08/02 | **【UI 优化】生成清单页字段清单 Tab 视觉与数据修复**：1) **分组名重复序号前缀修复**——excel-to-form 生成时分组名可能带多重中文序号前缀（`一、一、基础信息`），现 parseFieldsData 解析与 saveManifestFile 保存时均剥离重复前缀（保留单重），excel-to-form 的 `stripModuleNumberPrefix` 改为全局剥离所有重复前缀（源头修复）；2) **箭头方向修复**——展开显示 ▼（向下）、收起显示 ▶（向右），修正原先收起时 ▲（向上）的错误；3) **分组样式美化**——去掉分组标题左侧 `border-left: 4px` 加深标记，改用浅灰底色 + 白色卡片 + 圆角 + 柔和阴影，箭头/类型徽标用更现代的蓝/红色；4) `GENERATOR_VERSION` 2.19.0 → 2.20.0（页面级改动，触发全量重建） |
| 2.19.0 | 2026/08/02 | **【增强】生成清单页（manifest.html）字段清单 Tab 体验优化**：1) **使用说明 HTML 排版**——新增 `mdToHtml`/`inlineFormat`，将使用说明从 md 原文（`<pre>`）改为结构化 HTML（标题/表格/引用/加粗/分隔线），不再原样显示 md 语法；2) **分组/表单折叠与区分**——分组标题加粗 + 渐变色 + 边框高亮，表单带类型徽标，分组和表单均默认收起、可点击展开/收起，提供「全部收起/全部展开」按钮，搜索命中自动展开；3) **字段状态/必填下拉选项 + 自动保存**——字段状态（普通/只读/隐藏）与是否必填（是/否）由 contenteditable 改为下拉 `select`，选择后**自动保存并回写 md 源文件**（防抖 500ms）；4) 修复与 `app.js` 的 `toggleGroup` 函数名冲突，折叠函数加 `manifest` 前缀；5) `GENERATOR_VERSION` 2.18.0 → 2.19.0（页面级改动，触发 sync-ops 全量重建） |
| 2.18.0 | 2026/08/02 | **【关键修复】中文应用目录名 URL 编码 bug（根源修复，同步到生成器模板）**：1) 修复 `getAppDirName()`/`getAppNameFromPath()`/`projectDir` 计算逻辑——`window.location.pathname` 对中文目录名（如 `进销存3`）返回 URL 编码形式（`%E8%BF%9B...`），现统一 `decodeURIComponent` 还原为明文，否则 `buildFileParam` 拼接后 `encodeURIComponent` 双重编码导致同步服务 404、保存/刷新失败；2) 修复「组织主页」链接写死 `/index.html` 导致空白——改为 `/本地操作页面/index.html`（宜搭AI助手组织管理门户）；3) 所有 5 个 HTML 模板（index/list/form/guide/manifest）统一同步修复；4) **`GENERATOR_VERSION` 2.14.0 → 2.18.0**（页面级模板改动，必须递增以触发 sync-ops 对全部已存在应用的强制全量重建，否则其他应用保留旧 bug 页面） |
| 2.17.0 | 2026/07/17 | **【关键修复】同步功能应用匹配问题**：1) `syncForm` 函数新增 `projectDir` 参数传递，从页面URL自动计算项目目录；2) 支持 HTTP 和 file 协议两种访问方式；3) 后端 `findProjectDir` 优先使用传入的 `projectDir`，避免多个应用有相同表单名时匹配错误；4) 修复后确保进销存1/2/3/5/8等任意数量应用都能正确同步各自表单 |
| 2.16.0 | 2026/07/07 | **路径兼容性修复**：1) `loadFormListFromConfig` 总是用最新分组信息覆盖 formPaths（修复旧版原型页面路径不匹配问题）；2) `loadFormConfig` fetch 失败时回退到 staticConfigData 而非返回空配置；3) 分组目录加「分组」后缀对齐 |
| 2.15.0 | 2026/07/07 | **菜单图标与折叠优化**：1) 无分组表单（含 `系统配置清单.md` 第5列 `-`/`—` 占位符）不强行分组，直接以扁平项显示带 📄 图标；2) 分组标题加 📁 文件夹图标和 ▼/▲ 上下箭头，点击可折叠/展开；3) 普通表单用 📄 图标、流程表单用 🔄 图标替代原 ▸ 绿色箭头；4) 移除 `.menu-item.process::before` CSS 规则；5) 新增 `toggleGroup()`/`getFormIcon()` 函数；6) 多层防御性归一化 `-`/`—` 占位符（`loadFormListFromConfig`/`generateStaticConfigData`/`generateFormConfigDataJs`/`renderMenu`） |
| 2.14.0 | 2026/07/07 | **菜单按分组渲染**：`renderMenu()` 按 `group` 字段聚合输出多个 `.menu-group` 块；配置数据追加 `group` 字段并回填；4 个 HTML 模板移除硬编码 `业务表单` 包裹；`renderQuickLinks` 卡片显示分组名 |
